% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_raincloud}
\alias{mark_raincloud}
\title{Raincloud plot}
\usage{
mark_raincloud(plot, x, y, ..., width = 0.4, alpha = 0.5)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{x, y}{The categorical \code{x} and the sample \code{y} (tidy-eval).}

\item{...}{Further shared encodings (e.g. \code{color}, \code{fill}) forwarded to both
the slab and the points.}

\item{width}{Sina spread of the rain, as a fraction of the band (default
\code{0.4}).}

\item{alpha}{Point opacity for the rain (default \code{0.5}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_raincloud()} is a convenience that composes a \strong{raincloud}: a one-sided
density "cloud" (\code{\link[=mark_halfeye]{mark_halfeye()}}) with the raw observations as "rain" below
it (\code{\link[=mark_point]{mark_point()}} with sina spread). It is exactly
\code{plot |> mark_halfeye(...) |> mark_point(..., position = position_sina())},
so any \code{color}/\code{fill} mapping in \code{...} flows to both layers.
}
\examples{
set.seed(1)
d <- data.frame(g = rep(c("a", "b", "c"), each = 100),
                y = rnorm(300, rep(c(0, 1, 2), each = 100)))
vplot(d) |> mark_raincloud(x = g, y = y)
}
\seealso{
\code{\link[=mark_halfeye]{mark_halfeye()}}, \code{\link[=position_sina]{position_sina()}}
}
