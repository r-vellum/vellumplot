% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_graph}
\alias{mark_graph}
\alias{mark_edges}
\alias{mark_nodes}
\alias{mark_node_text}
\alias{mark_edge_text}
\alias{mark_node_hull}
\alias{mark_node_pie}
\title{Network (graph) marks}
\usage{
mark_edges(
  plot,
  ...,
  color = NULL,
  linewidth = NULL,
  alpha = NULL,
  linetype = NULL,
  arrow = FALSE,
  routing = c("straight", "elbow"),
  elbow_at = c("mid", "start", "end"),
  elbow_axis = c("auto", "v", "h"),
  gradient = FALSE,
  auto = FALSE,
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)

mark_nodes(
  plot,
  ...,
  size = NULL,
  shape = NULL,
  fill = NULL,
  color = NULL,
  alpha = NULL,
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)

mark_node_text(
  plot,
  ...,
  label = NULL,
  color = NULL,
  size = NULL,
  alpha = NULL,
  dist = 0,
  top_n = NULL,
  by = NULL,
  repel = FALSE,
  box_padding = 1,
  point_padding = 1,
  min_segment_length = 2,
  max_overlaps = 10,
  seed = NULL,
  effects = list(),
  blend = NULL,
  data = NULL
)

mark_edge_text(
  plot,
  ...,
  label = NULL,
  color = NULL,
  size = NULL,
  alpha = NULL,
  angle = NULL,
  effects = list(),
  blend = NULL,
  data = NULL
)

mark_node_hull(
  plot,
  ...,
  fill = NULL,
  color = NULL,
  alpha = 0.25,
  expand = 0.08,
  data = NULL
)

mark_node_pie(
  plot,
  cols,
  ...,
  size = 4,
  inner = 0,
  fill = NULL,
  color = "white",
  linewidth = 0.5,
  alpha = NA,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}, normally from \code{\link[=vgraph]{vgraph()}}.}

\item{...}{Encodings mapping node/edge attributes to aesthetics. Nodes: \code{size},
\code{color}/\code{fill}, \code{shape}, \code{alpha}. Edges: \code{color}, \code{linewidth}, \code{linetype},
\code{alpha}. The position channels are supplied by \code{vgraph()} and need not be
mapped.}

\item{linewidth}{For \code{mark_edges()}, the edge width; a constant or (via
\code{\link[=scale_edge_width]{scale_edge_width()}}) a mapped expression such as \code{linewidth = weight}. For
\code{mark_node_pie()}, the wedge border width.}

\item{linetype}{For \code{mark_edges()}, the edge line type; a constant or (via
\code{\link[=scale_edge_linetype]{scale_edge_linetype()}}) a mapped expression.}

\item{arrow}{For \code{mark_edges()}, \code{TRUE} to draw a closed arrowhead at each
edge's target end (the directed convention), \code{FALSE}/\code{NULL} for none, or a
\code{\link[vellum:vl_arrow]{vellum::vl_arrow()}} spec for full control (\code{ends}, \code{type}, \code{length},
\code{angle}) -- e.g. \code{arrow = vellum::vl_arrow(ends = "both", type = "open")}.
Edges are capped exactly at each endpoint's node boundary (per vertex, at any
size/resolution), so the head sits on the node edge; self-loops are drawn as
teardrop loops sized to the node, with the head on the node boundary.}

\item{routing}{For \code{mark_edges()}, edge routing: \code{"straight"} (default) or
\code{"elbow"} -- orthogonal right-angle steps for tree / DAG / dendrogram
layouts (still straight segments, no curvature). Elbows keep node-boundary
caps and arrowheads.}

\item{elbow_at, elbow_axis}{For \code{mark_edges(routing = "elbow")}, the corner
placement and axis. \code{elbow_at} is \code{"mid"} (default, an S-bend at the
midpoint), or \code{"start"} / \code{"end"} for the corner at the source / target --
\code{"start"} gives the dendrogram \emph{bracket} (siblings share a bar at the
parent's level). \code{elbow_axis} is \code{"auto"} (default, the longer side),
\code{"v"}, or \code{"h"} to force which axis the corner steps along.}

\item{gradient}{For \code{mark_edges()}, \code{TRUE} to fade each (straight) edge from
faint at its source to opaque at its target -- a direction cue that needs no
arrowhead (igraph's \code{edge.gradient}). Ignored with \code{routing = "elbow"}.}

\item{auto}{For \code{mark_edges()}, \code{TRUE} to datashade a large graph's edges as a
density raster (\code{\link[vellum:datashade_segments]{vellum::datashade_segments()}}) once the edge count exceeds
the datashade threshold, instead of drawing each edge as a vector segment —
the fast, overplotting-honest path for hairballs. The device-space refinements
of the vector path (parallel-edge offsets, node-boundary caps, arrowheads,
teardrop self-loops) do not apply to the rasterised edges.}

\item{blend}{Optional blend mode (see \code{\link[=mark_point]{mark_point()}}).}

\item{effects}{A list of layer render effects (\code{\link[=glow]{glow()}}, \code{\link[=outline]{outline()}},
\code{\link[=shadow]{shadow()}}) applied to the mark at draw time. On the text marks
(\code{mark_node_text()} / \code{mark_edge_text()}) only \code{\link[=shadow]{shadow()}} applies.}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving the layer a hand-drawn look,
\code{NA}/\code{FALSE} to force it crisp, or \code{NULL} (default) to inherit.}

\item{data}{Optional layer data; overrides the default table.}

\item{size, shape}{For \code{mark_nodes()}, the node size (mm) / shape; a constant or
a mapped expression. \code{size} is also the label font size for
\code{mark_node_text()}/\code{mark_edge_text()}.}

\item{fill, color, alpha}{Convenience aesthetics; a constant or a mapped
expression. For nodes, \code{fill} (or \code{color}) is the marker colour; for
\code{mark_edges()}, \code{color}/\code{alpha} map through the edge scales.}

\item{label}{For \code{mark_node_text()}, the label expression (default the vertex
\code{name}); for \code{mark_edge_text()}, the edge label expression (no default -- map
an edge attribute, e.g. \code{label = weight}).}

\item{dist}{For \code{mark_node_text()}, a radial offset (mm) pushing each label
outward from the layout centroid, so labels clear the node markers instead of
sitting on them. \code{0} (default) centres the label on the vertex.}

\item{top_n, by}{For \code{mark_node_text()}, label only the \code{top_n} vertices with
the largest \code{by} (an edge/vertex metric column, e.g. \code{by = degree}) -- the
idiomatic "label just the hubs" filter. \code{NULL} (default) labels every vertex.}

\item{repel}{For \code{mark_node_text()}, \code{TRUE} to move overlapping labels apart
with a force-directed layout (ggrepel-style), drawing a thin leader line back
to each vertex. \code{box_padding}, \code{point_padding}, \code{min_segment_length},
\code{max_overlaps}, and \code{seed} tune it exactly as in \code{\link[=mark_text]{mark_text()}}.}

\item{box_padding, point_padding, min_segment_length, max_overlaps, seed}{Repel
tuning for \code{mark_node_text(repel = TRUE)}; see \code{\link[=mark_text]{mark_text()}}.}

\item{angle}{For \code{mark_edge_text()}, the label rotation: a constant in degrees,
or \code{"along"} to rotate each label along its edge. \code{NULL} (default) draws
horizontal labels.}

\item{expand}{For \code{mark_node_hull()}, the fraction to grow each hull outward
from its centroid so it encloses the node markers (default \code{0.08}).}

\item{cols}{For \code{mark_node_pie()}, the compositional columns (a character
vector of node-table column names, at least two) whose values size each
node's wedges.}

\item{inner}{For \code{mark_node_pie()}, the inner-radius fraction: \code{0} (default)
draws a pie, \verb{> 0} a donut (e.g. \code{0.5}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Draw a node-link diagram on a \link{PlotSpec} from \code{\link[=vgraph]{vgraph()}}. \code{mark_edges()} draws
the edges (straight lines, batched), \code{mark_nodes()} the vertices (points),
\code{mark_node_text()} the vertex labels, and \code{mark_edge_text()} labels on the
edges. Draw order is fixed regardless of the order you pipe them: edges under
edge labels under nodes under node labels. Edges (and edge labels) default to
the edge table (\code{vgraph()}'s \code{edge_data}), nodes and node labels to the node
table; the \code{x}/\code{y}/\code{xend}/\code{yend}/\code{label}/\code{name} columns those tables carry are
mapped automatically, so bare \code{mark_edges() |> mark_nodes()} just works.
}
\details{
Edge colour, opacity, and line type train on \strong{their own scales}
(\code{\link[=scale_edge_color]{scale_edge_color()}}, \code{\link[=scale_edge_alpha]{scale_edge_alpha()}}, \code{\link[=scale_edge_linetype]{scale_edge_linetype()}}, plus
\code{\link[=scale_edge_width]{scale_edge_width()}}), independent of the node colour/alpha/linetype scales --
so a plot can map node fill to a discrete community and edge colour to a
continuous weight without the two legends colliding.

\code{mark_node_text()} has the label-legibility tools a dense graph needs:
\code{repel = TRUE} nudges overlapping labels apart with leader lines, \code{dist} pushes
labels radially clear of the node markers, \code{top_n}/\code{by} label only the hubs,
and \code{effects = list(shadow())} drops a shadow behind each label so it stays
readable over edges. \code{mark_edge_text()} takes the same \code{effects}. (Only
\code{\link[=shadow]{shadow()}} applies to text; a glyph outline / glow is not available yet.)

Two more decorations: \code{mark_node_hull()} shades communities with a convex hull
behind the graph (grouped by a mapped \code{fill}, drawn under the edges), and
\code{mark_node_pie()} replaces node markers with pie / donut glyphs whose wedges
come from a set of compositional columns.

These are thin over the point / segment / text marks; \code{igraph} need not be
installed to use them (only \code{\link[=vgraph]{vgraph()}} needs it).
}
\examples{
\dontrun{
g <- igraph::make_graph("Zachary")
vgraph(g) |>
  mark_edges(alpha = 0.5) |>
  mark_nodes(size = 4, fill = "steelblue")
}
}
\seealso{
\code{\link[=vgraph]{vgraph()}}, \code{\link[=scale_edge_width]{scale_edge_width()}}, \code{\link[=scale_edge_color]{scale_edge_color()}}
}
