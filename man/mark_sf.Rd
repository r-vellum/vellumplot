% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_sf}
\alias{mark_sf}
\title{Draw simple-feature (sf) geometries}
\usage{
mark_sf(
  plot,
  ...,
  fill = NULL,
  color = NULL,
  alpha = NULL,
  linewidth = NULL,
  size = NULL,
  na_value = "grey80",
  merge = FALSE,
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings mapping feature attributes to aesthetics: \code{fill},
\code{color}, \code{alpha}, \code{linewidth}, \code{size}. A geometry column is not encoded —
it is read from the data.}

\item{fill, color, alpha, linewidth, size}{Convenience aesthetic arguments; a
constant or a mapped expression.}

\item{na_value}{Fill colour for features whose mapped \code{fill}/\code{color} value is
\code{NA} (drawn as a distinct legend swatch). Default \code{"grey80"}.}

\item{merge}{Dissolve adjacent features that share a fill into one region, so
the internal borders between same-valued features disappear and each region
is a single crisp path (no seam artefacts, and exact in PDF). The union is
real boolean geometry via \code{\link[vellum:vl_path_op]{vellum::vl_path_op()}}. Default \code{FALSE}. Ignored
when the layer is interactive (merging would drop the per-feature keys).}

\item{blend}{Optional blend mode (see \code{\link[=mark_point]{mark_point()}}).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving the layer a hand-drawn look, or \code{NULL}
(default) to inherit.}

\item{data}{Optional layer data (an \code{sf} object); overrides the plot data.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_sf()} draws the geometry column of an \code{sf} object as a map layer:
\code{POINT}/\code{MULTIPOINT} render as points, \code{LINESTRING}/\code{MULTILINESTRING} as
polylines, and \code{POLYGON}/\code{MULTIPOLYGON} as filled paths (holes cut with the
even-odd rule, so ring winding need not be canonical). Coordinates come from
the geometry, so there are no \code{x}/\code{y} encodings; other aesthetics map feature
attributes as usual, e.g. \code{fill = AREA} for a choropleth. Pair with
\code{\link[=coord_sf]{coord_sf()}} to reproject and lock the map aspect ratio.
}
\details{
\code{sf} is an optional dependency (in \code{Suggests}); \code{mark_sf()} errors with an
install hint if it is not available.
}
\examples{
\dontrun{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
vplot(nc) |> mark_sf(fill = BIR74) |> coord_sf()
}
}
\seealso{
\code{\link[=coord_sf]{coord_sf()}}, \code{\link[=scale_fill_binned]{scale_fill_binned()}}
}
