% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales-binned.R
\name{scale_x_binned}
\alias{scale_x_binned}
\alias{scale_y_binned}
\title{Binned position scales}
\usage{
scale_x_binned(
  plot,
  style = "pretty",
  n = 10,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_y_binned(
  plot,
  style = "pretty",
  n = 10,
  breaks = NULL,
  labels = NULL,
  name = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{style}{Binning style: \code{"pretty"} (default), \code{"equal"}, \code{"quantile"}, or a
\code{classInt} style.}

\item{n}{Target number of bins.}

\item{breaks}{Explicit bin boundaries (overrides \code{style}/\code{n}), or \code{NULL}.}

\item{labels}{Explicit boundary labels, or \code{NULL} to format the boundaries.}

\item{name}{Axis title, or \code{NULL} to derive from the encoding.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{scale_x_binned()} / \code{scale_y_binned()} cut a continuous position variable into
bins: axis ticks fall at the bin \strong{boundaries} and each datum is drawn at its
bin \strong{centre}. Breaks use the same classification as the binned colour scales
(\code{style}/\code{n}; \code{classInt} for jenks/fisher/… — a Suggest). Handy to summarise a
dense continuous axis, or to place \code{mark_bar()} on a binned continuous \code{x}.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> scale_x_binned(n = 6)
}
