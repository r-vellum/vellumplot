% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{inset}
\alias{inset}
\title{Overlay a plot as an inset}
\usage{
inset(
  base,
  plot,
  left = 0.6,
  bottom = 0.6,
  right = 0.98,
  top = 0.98,
  align_to = c("panel", "plot", "full"),
  on_top = TRUE
)
}
\arguments{
\item{base}{A \link{PlotSpec} or \code{PlotComposition} to draw underneath.}

\item{plot}{The \link{PlotSpec} to overlay.}

\item{left, bottom, right, top}{Inset position as fractions (0–1).}

\item{align_to}{Reference box: \code{"panel"}, \code{"plot"}, or \code{"full"} (currently the
whole \code{base} cell).}

\item{on_top}{Draw the inset above (\code{TRUE}) or below the base.}
}
\value{
A \code{PlotComposition}.
}
\description{
Place \code{plot} as an inset over \code{base}, positioned by fractional coordinates
(0–1) of the chosen reference box. Returns a 1-cell \code{PlotComposition} carrying
the inset (compose it further or render directly).
}
\examples{
a <- vplot(mtcars) |> mark_point(x = wt, y = mpg)
b <- vplot(mtcars) |> mark_histogram(x = mpg, bins = 8)
inset(a, b, left = 0.55, bottom = 0.55, right = 0.98, top = 0.98)
}
