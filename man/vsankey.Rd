% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sankey.R
\name{vsankey}
\alias{vsankey}
\alias{mark_sankey}
\title{Sankey (flow) diagram}
\usage{
vsankey(
  data,
  from,
  to,
  value,
  label = TRUE,
  show_values = FALSE,
  flow_color = "source",
  node_width = 0.04,
  node_gap = 0.02,
  width = 8,
  height = 5,
  dpi = 96
)

mark_sankey(
  plot,
  from,
  to,
  value,
  label = TRUE,
  show_values = FALSE,
  flow_color = "source",
  node_width = 0.04,
  node_gap = 0.02
)
}
\arguments{
\item{data}{A data frame of flows.}

\item{from, to, value}{Columns (tidy-eval): the source node, target node, and
flow value.}

\item{label}{Draw node labels? Default \code{TRUE}.}

\item{show_values}{Append each node's value to its label (e.g. \code{"Grid (60)"})?
Default \code{FALSE}. Ignored when \code{label = FALSE}.}

\item{flow_color}{Ribbon fill: \code{"source"} (default) colours each ribbon by its
source node, \code{"target"} by its target node, and \code{"gradient"} fades each
ribbon from its source colour to its target colour.}

\item{node_width}{Node-rectangle width, as a fraction of the plotting width
(default \code{0.04}).}

\item{node_gap}{Vertical gap between the nodes in a column, as a fraction of
the column height (default \code{0.02}).}

\item{width, height, dpi}{Page size (inches) and resolution.}

\item{plot}{A \link{PlotSpec}.}
}
\value{
A \link{PlotSpec} (\code{vsankey()}) or the modified plot (\code{mark_sankey()}).
}
\description{
\code{vsankey()} draws a layered flow diagram from a \emph{flow list} — one row per
flow with a \code{from} node, a \code{to} node, and a \code{value} (the ribbon width). Nodes
are the union of \code{from}/\code{to}; a node that is both a source and a target makes
the diagram multi-stage. Like \code{\link[=vgraph]{vgraph()}}, it returns a ready \link{PlotSpec} with an
axis-free, aspect-free panel; \code{mark_sankey()} is the layer it adds and can be
used directly on a plot you have set up yourself.
}
\details{
The flows must form a DAG (no cycles). Nodes within a column are ordered to
minimise ribbon crossings (a deterministic Sugiyama barycenter sweep), and
ribbons are stacked to meet each node in matching order. Nodes are coloured
from the built-in qualitative palette.
}
\examples{
flows <- data.frame(
  from = c("A", "A", "B", "C", "C"),
  to = c("B", "C", "D", "D", "E"),
  value = c(4, 6, 4, 4, 2)
)
vsankey(flows, from, to, value)
vsankey(flows, from, to, value, show_values = TRUE, flow_color = "target")
}
