% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_linewidth}
\alias{scale_linewidth}
\alias{scale_linewidth_continuous}
\title{Line-width scale}
\usage{
scale_linewidth(plot, range = NULL, limits = NULL, breaks = NULL, name = NULL)

scale_linewidth_continuous(
  plot,
  range = NULL,
  limits = NULL,
  breaks = NULL,
  name = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{range}{Output line-width range \code{c(min, max)}, or \code{NULL} for the
default \code{c(0.5, 4)}.}

\item{limits}{Data limits \code{c(min, max)}, or \code{NULL} to train from the data.}

\item{breaks}{Explicit legend breaks, or \code{NULL}.}

\item{name}{Legend title, or \code{NULL} to derive from the encoding.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Declare the scale for a mapped \code{linewidth} aesthetic on a stroked mark --
\code{mark_line()}, \code{mark_step()}, or \code{mark_segment()}. Data values are rescaled
linearly onto a line-width \code{range} (in \code{lwd} units, as everywhere else in the
package) and a line-width legend is drawn automatically.
}
\details{
A mapped \code{linewidth} gives each \strong{segment} one constant width, taken as the
mean of its two endpoint values. So the width changes in visible steps at each
vertex rather than tapering smoothly along the line, and the round joins do
not perfectly fill the corners of a sharp bend. That is inherent to
per-segment width; a constant \verb{linewidth =} is unaffected and still draws the
line as a single stroke.

No backend can stroke the segments of one path at different widths in a single
call, so a mapped \code{linewidth} emits one path element per segment in SVG and
PDF. On a long, dense line that is a lot of elements -- worth knowing before
mapping \code{linewidth} on tens of thousands of rows.

For edges on a \code{\link[=vgraph]{vgraph()}} plot, use \code{\link[=scale_edge_width]{scale_edge_width()}} instead: an edge's
width is trained and legended independently of the line-width scale.
}
\examples{
vplot(pressure) |>
  mark_line(x = temperature, y = pressure, linewidth = pressure) |>
  scale_linewidth(range = c(0.5, 5))
}
\seealso{
\code{\link[=mark_line]{mark_line()}}, \code{\link[=mark_step]{mark_step()}}, \code{\link[=mark_segment]{mark_segment()}}, \code{\link[=scale_edge_width]{scale_edge_width()}}
}
