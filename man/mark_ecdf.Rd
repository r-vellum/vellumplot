% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_ecdf}
\alias{mark_ecdf}
\alias{mark_rug}
\alias{mark_qq}
\alias{mark_qq_line}
\title{Distribution marks}
\usage{
mark_ecdf(plot, ..., blend = NULL, sketch = NULL, data = NULL)

mark_rug(plot, ..., sides = "bl", length = 0.03, blend = NULL, data = NULL)

mark_qq(
  plot,
  ...,
  distribution = "qnorm",
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_qq_line(
  plot,
  ...,
  distribution = "qnorm",
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{...}{Encodings. \code{mark_ecdf()} needs \code{x}; \code{mark_qq()}/\code{mark_qq_line()}
need \code{sample}; \code{mark_rug()} takes \code{x} and/or \code{y} (+ \code{color}, \code{alpha},
\code{linewidth}, \code{linetype}).}

\item{blend, sketch, data}{Standard layer arguments (see \code{\link[=mark_point]{mark_point()}}).}

\item{sides}{Which edges \code{mark_rug()} draws ticks on: any of \code{"b"} (bottom),
\code{"l"} (left), \code{"t"} (top), \code{"r"} (right); default \code{"bl"}.}

\item{length}{Rug tick length as a fraction of the panel (default \code{0.03}).}

\item{distribution}{Quantile function of the reference distribution for
\code{mark_qq()} / \code{mark_qq_line()} (default \link[stats:qnorm]{stats::qnorm}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Marks that summarise the distribution of a variable.
\code{mark_ecdf()} draws the empirical cumulative distribution of \code{x} as a step;
\code{mark_rug()} draws marginal ticks at each datum; \code{mark_qq()} draws a
quantile-quantile plot of a \code{sample} against a theoretical distribution, with
\code{mark_qq_line()} adding the reference line. All respect a mapped \code{color}/\code{fill}
grouping.
}
\examples{
vplot(mtcars) |> mark_ecdf(x = mpg)
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> mark_rug()
vplot(mtcars) |> mark_qq(sample = mpg) |> mark_qq_line(sample = mpg)
}
