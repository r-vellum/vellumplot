% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg.R
\name{gt_vsparkline}
\alias{gt_vsparkline}
\title{Add a vellumplot sparkline column to a \code{gt} table}
\usage{
gt_vsparkline(
  gt_object,
  column,
  type = "line",
  ...,
  width = 30,
  height = 8,
  units = "mm"
)
}
\arguments{
\item{gt_object}{A \code{\link[gt:gt]{gt::gt()}} object.}

\item{column}{The list-column to render (bare name or string).}

\item{type}{Sparkline type: \code{"line"} (default), \code{"bar"}, or \code{"winloss"}.}

\item{...}{Further \code{\link[=vsparkline]{vsparkline()}} arguments (e.g. \code{color}, \code{points}).}

\item{width, height, units}{Sparkline size (default \verb{30 x 8} mm).}
}
\value{
The modified \code{gt} object.
}
\description{
\code{gt_vsparkline()} renders a \strong{list-column of numeric vectors} as a per-row
\code{\link[=vsparkline]{vsparkline()}}, embedded as inline SVG in a \code{\link[gt:gt]{gt::gt()}} table — so a gt table
(with all of gt's formatting and theming) gains a vellum sparkline column.
Needs the \pkg{gt} package, and HTML output (inline SVG is not embedded by gt's
LaTeX / Word backends).
}
\details{
The vectors are recovered from the gt object's data in its current row order;
apply \code{gt_vsparkline()} \strong{before} any gt row reordering / grouping.
}
\examples{
\dontrun{
df <- data.frame(metric = c("A", "B"))
df$trend <- list(cumsum(rnorm(20)), cumsum(rnorm(20)))
gt::gt(df) |> gt_vsparkline(trend, type = "line")
}
}
\seealso{
\code{\link[=vsparkline]{vsparkline()}}, \code{\link[=vtable]{vtable()}}, \code{\link[=plot_svg]{plot_svg()}}
}
