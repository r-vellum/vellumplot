% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vplot.R
\name{vplot}
\alias{vplot}
\title{Start a plot specification}
\usage{
vplot(data, width = 6, height = 4, dpi = 96)
}
\arguments{
\item{data}{A data frame. Encoding expressions in \verb{mark_*()} are evaluated
against it with tidy evaluation.}

\item{width, height}{Page size in inches.}

\item{dpi}{Output resolution in dots per inch. The exported PNG's pixel
dimensions are \code{width * dpi} by \code{height * dpi}; raising \code{dpi} yields a
denser image at the same physical size. Overridable at render time via
\code{\link[=render_plot]{render_plot()}}'s \code{dpi} argument.}
}
\value{
A \link{PlotSpec}.
}
\description{
\code{vplot()} begins a declarative, pipe-first plot. It captures the data and
page size and returns an inspectable \link{PlotSpec}; nothing is drawn until the
spec is compiled (via \code{\link[=render_plot]{render_plot()}} or \code{\link[vellum:as_vellum_scene]{vellum::as_vellum_scene()}}). Build
the plot up with \code{\link[=mark_point]{mark_point()}} / \code{\link[=mark_line]{mark_line()}} / \code{\link[=mark_rule]{mark_rule()}} and the
\verb{scale_*()} functions.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg)
}
