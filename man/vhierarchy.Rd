% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{vhierarchy}
\alias{vhierarchy}
\alias{mark_hierarchy}
\title{Hierarchy diagrams: sunburst, icicle, treemap, circle-pack}
\usage{
vhierarchy(
  data,
  id,
  parent,
  value,
  fill = NULL,
  type = c("sunburst", "icicle", "treemap", "circlepack"),
  inner_radius = 0,
  flow = c("down", "up", "right", "left"),
  lighten = 0.6,
  label = TRUE,
  show_values = FALSE,
  orientation = c("auto", "radial", "tangential", "horizontal"),
  root_label = FALSE,
  width = 6,
  height = 6,
  dpi = 96
)

mark_hierarchy(
  plot,
  id,
  parent,
  value,
  fill = NULL,
  type = c("sunburst", "icicle", "treemap", "circlepack"),
  inner_radius = 0,
  flow = c("down", "up", "right", "left"),
  lighten = 0.6,
  label = TRUE,
  show_values = FALSE,
  orientation = c("auto", "radial", "tangential", "horizontal"),
  root_label = FALSE
)
}
\arguments{
\item{data}{A data frame describing a hierarchy (a parent list).}

\item{id, parent, value}{Columns (tidy-eval): the node id, its parent id
(\code{NA}/\code{""} for the root), and its value (used for leaves).}

\item{fill}{Optional column (tidy-eval) to colour nodes by. Unmapped
(default), nodes are coloured by their depth-1 branch and lightened with
depth; mapped, each node takes its \code{fill} value's colour with no depth fade.}

\item{type}{Diagram geometry: \code{"sunburst"} (default), \code{"icicle"}, \code{"treemap"},
or \code{"circlepack"}.}

\item{inner_radius}{Sunburst only: central hole radius, a fraction in
\verb{[0, 1)}; \code{0} (default) fills to the centre.}

\item{flow}{Icicle only: the direction of increasing depth — \code{"down"}
(default), \code{"up"}, \code{"right"}, or \code{"left"}.}

\item{lighten}{Branch mode only: how far the deepest level fades toward white,
a fraction in \verb{[0, 1]} (default \code{0.6}; \code{0} = flat colour per branch).
Ignored when \code{fill} is mapped.}

\item{label}{Label each node with its \code{id}? Default \code{TRUE}. Nodes too small
for their label are left unlabelled.}

\item{show_values}{Append each node's value to its label, e.g. \code{"A1 (3)"}.
Default \code{FALSE}.}

\item{orientation}{Sunburst labels only: \code{"auto"} (default) angles each label
tangentially / radially to fit its wedge, or force \code{"radial"},
\code{"tangential"}, or \code{"horizontal"}. Labels are kept upright.}

\item{root_label}{Write the root's name (and, with \code{show_values}, its total)
in the centre? Default \code{FALSE}. Most useful for \code{"sunburst"}.}

\item{width, height, dpi}{Page size (inches) and resolution.}

\item{plot}{A \link{PlotSpec}.}
}
\value{
A \link{PlotSpec} (\code{vhierarchy()}) or the modified plot (\code{mark_hierarchy()}).
}
\description{
\code{vhierarchy()} draws a tree as a space-filling diagram, choosing the geometry
with \code{type}: \code{"sunburst"} (concentric ring sectors), \code{"icicle"} (rectangular
partition), \code{"treemap"} (squarified nested rectangles), or \code{"circlepack"}
(circles within circles). All four take the same \emph{parent list} — \code{id},
\code{parent} (\code{NA}/\code{""} at the root), and \code{value} (leaf values; an internal node's
value is the sum of its children) — so switching \code{type} re-encodes the same
data. Like \code{\link[=vgraph]{vgraph()}} / \code{\link[=vsankey]{vsankey()}} it returns a ready, axis-free,
aspect-locked \link{PlotSpec}; \code{mark_hierarchy()} is the layer it adds.
}
\details{
The root is structural and never drawn (in a sunburst it is the centre). Each
node is labelled with its \code{id} where the label fits, and \code{show_values} appends
the value; small nodes are left unlabelled so a dense diagram stays legible.
}
\section{Colour}{
By default nodes are coloured by their depth-1 \emph{branch} and lightened one step
per level, so sibling branches stay distinct and depth reads as shade. The
branch is an ordinary discrete fill scale, so \verb{scale_fill_*()} recolours the
branches (e.g. \code{scale_fill_brewer()}), and \code{lighten} controls the depth fade
(\code{0} = flat colour per branch). Map \code{fill} to a node column instead to colour
every node by that variable — discrete or continuous, with the matching
\verb{scale_fill_*()} — in which case the depth fade is not applied.
}

\examples{
h <- data.frame(
  id = c("root", "A", "B", "A1", "A2", "B1"),
  parent = c(NA, "root", "root", "A", "A", "B"),
  value = c(NA, NA, NA, 3, 2, 4)
)
vhierarchy(h, id, parent, value, show_values = TRUE)
vhierarchy(h, id, parent, value, type = "treemap")
}
