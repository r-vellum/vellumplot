% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{provenance_join}
\alias{provenance_join}
\title{Join a plot's provenance to its rendered geometry}
\usage{
provenance_join(x)
}
\arguments{
\item{x}{A \link{PlotSpec} or composition.}
}
\value{
A data frame: \code{id}, \code{layer}, \code{mark}, \code{kind}, \code{panel}, the pixel bbox
(\code{x0}, \code{y0}, \code{x1}, \code{y1}, \code{x}, \code{y}, \code{w}, \code{h}), \code{n_rows}, and \code{rows} (a list
column of integer source-row indices). Empty for a plot with no mark grobs.
}
\description{
\code{provenance_join()} compiles \code{x} once and returns a tidy table with one row
per emitted mark element, tying each drawn primitive to \strong{both} the source
data rows that produced it (\code{rows}, a list column) \strong{and} its device-pixel
geometry (\code{x0}, \code{y0}, \code{x1}, \code{y1}, \code{x}, \code{y}, \code{w}, \code{h}) from
\code{\link[vellum:scene_model]{vellum::scene_model()}}. It is the consumer of the provenance table that
\code{\link[=plot_provenance]{plot_provenance()}} emits — the substrate for click-to-source interactivity,
auditing "which rows made this element?", and linked views.
}
\examples{
df <- data.frame(wt = mtcars$wt, mpg = mtcars$mpg, cyl = factor(mtcars$cyl))
p <- vplot(df) |> mark_point(x = wt, y = mpg, color = cyl)
pj <- provenance_join(p)
pj[, c("id", "mark", "n_rows")]
}
\seealso{
\code{\link[=plot_provenance]{plot_provenance()}}, \code{\link[vellum:scene_model]{vellum::scene_model()}}
}
