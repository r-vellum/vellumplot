% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{vtable}
\alias{vtable}
\title{Tables with sparkline columns}
\usage{
vtable(
  data,
  spark = list(),
  cols = NULL,
  align = NULL,
  header = TRUE,
  font_size = 9,
  spark_width = 24,
  row_height = 7,
  cell_pad = 1.5,
  units = "mm",
  dpi = 96
)
}
\arguments{
\item{data}{A data frame. Sparkline columns must be \strong{list-columns} whose
cells are numeric vectors.}

\item{spark}{A named list mapping a (list-)column name to how it draws: a
\code{type} string (\code{"line"}/\code{"bar"}/\code{"winloss"}) or a function
\verb{function(values)} returning a \code{\link[=vsparkline]{vsparkline()}}.}

\item{cols}{Character vector of columns to show, in order (default all).}

\item{align}{Optional named list of per-column alignment
(\code{"left"}/\code{"right"}/\code{"centre"}); defaults to right for numeric, left otherwise.}

\item{header}{Draw a bold header row with an underline (default \code{TRUE}).}

\item{font_size}{Text size in points (default \code{9}).}

\item{spark_width}{Width of a sparkline column, in \code{units} (default \code{24}).}

\item{row_height}{Row height, in \code{units} (default \code{7}).}

\item{cell_pad}{Horizontal padding inside a text cell, in \code{units} (default \code{1.5}).}

\item{units}{Length unit for the sizes above: \code{"mm"} (default), \code{"cm"}, \code{"in"},
\code{"pt"}.}

\item{dpi}{Resolution for raster output.}
}
\value{
A \code{VTable} (renders via \code{\link[=render_plot]{render_plot()}}).
}
\description{
\code{vtable()} lays a data frame out as a grid of cells where an ordinary column
renders as text and a \strong{list-column of numeric vectors} renders as a
per-row \code{\link[=vsparkline]{vsparkline()}} — chart-in-table, drawn as one vector scene (so it
renders on PNG / SVG / PDF like any plot). It returns a compiled object;
\code{\link[=render_plot]{render_plot()}} it, \code{print()} it, or drop it into a composition.
}
\examples{
df <- data.frame(name = c("A", "B", "C"), mean = c(3.1, 5.4, 2.2))
df$trend <- list(cumsum(rnorm(20)), cumsum(rnorm(20)), cumsum(rnorm(20)))
vtable(df, spark = list(trend = "line"))
}
\seealso{
\code{\link[=vsparkline]{vsparkline()}}
}
