% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-serialize.R
\name{as_spec}
\alias{as_spec}
\alias{from_spec}
\title{Serialize a plot to a plain spec (and back)}
\usage{
as_spec(plot)

from_spec(spec, data = NULL, env = globalenv())
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{spec}{A spec list from \code{as_spec()}.}

\item{data}{Optional data frame to render the spec against. Required when the
spec stores its data by reference; for a spec that inlines its data (or has
no data block), a supplied \code{data} takes precedence over the inlined copy.}

\item{env}{The environment channel expressions are re-quoted in (default the
global environment).}
}
\value{
\code{as_spec()} returns a named list; \code{from_spec()} returns a \link{PlotSpec}.
}
\description{
\code{as_spec()} turns a \link{PlotSpec} into a plain, nested, \strong{serializable} R list
(data, layers, encodings, scales, coordinates, facets, labels, page size);
\code{from_spec()} rebuilds a \code{PlotSpec} from that list. Together with
\code{\link[=spec_to_json]{spec_to_json()}} / \code{\link[=spec_from_json]{spec_from_json()}} this makes a vellumplot plot a portable
\emph{document} — the substrate for the LLM / agent tooling (\code{\link[=spec_fields]{spec_fields()}},
\code{\link[=vplot_from_spec]{vplot_from_spec()}}) and the Vega-Lite bridge (\code{\link[=spec_to_vegalite]{spec_to_vegalite()}}).
}
\details{
The spec is a \strong{subset} of the full grammar — the encoding-level state that a
portable document can carry faithfully. State that a JSON document cannot
represent (custom transform \emph{functions}, paint/pattern fills, hand-drawn
\code{\link[=sketch]{sketch()}} geometry, secondary axes, \code{sf} CRS objects, per-layer data) is
\strong{refused} with a classed \code{vellumplot_unserializable} error naming the exact
slot — it is never silently dropped. The single exception is theme element
customisation, which is styling orthogonal to the data spec: the theme
\emph{preset} name and its scalar settings round-trip, but custom \verb{element_*()}
overrides are dropped with a warning.

Channel expressions are stored as text (a column name, or an expression like
\code{log(x)}); the quosure's captured environment is dropped, so an encoding that
closes over a local variable will not round-trip.

Data is \strong{inlined} when small (at most a few thousand cells and all-atomic
columns) and otherwise stored \strong{by reference} (name + content hash + column
schema); a by-reference spec needs \code{from_spec(spec, data = )} to recompile.
}
\examples{
p <- vplot(mtcars) |> mark_point(x = wt, y = mpg, color = cyl)
spec <- as_spec(p)
spec$layers[[1]]$mark
p2 <- from_spec(spec)
}
\seealso{
\code{\link[=spec_to_json]{spec_to_json()}}, \code{\link[=spec_to_vegalite]{spec_to_vegalite()}}, \code{\link[=vplot_from_spec]{vplot_from_spec()}}
}
