% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_boxplot}
\alias{mark_boxplot}
\alias{mark_errorbar}
\alias{mark_linerange}
\alias{mark_summary}
\alias{mark_pointrange}
\alias{mark_crossbar}
\title{Boxplot, error bar, and summary marks}
\usage{
mark_boxplot(plot, ..., blend = NULL, sketch = NULL, data = NULL)

mark_errorbar(plot, ..., width = 0.5, blend = NULL, sketch = NULL, data = NULL)

mark_linerange(plot, ..., blend = NULL, sketch = NULL, data = NULL)

mark_summary(plot, ..., fun = mean, blend = NULL, sketch = NULL, data = NULL)

mark_pointrange(plot, ..., blend = NULL, sketch = NULL, data = NULL)

mark_crossbar(plot, ..., width = 0.5, blend = NULL, sketch = NULL, data = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y} for boxplot/summary; \code{x}, \code{ymin},
\code{ymax} for errorbar/linerange; plus \code{color}/\code{fill} (and \code{linetype} for
errorbar/linerange).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}

\item{width}{For \code{mark_errorbar()}, the cap width as a fraction of the band.}

\item{fun}{For \code{mark_summary()}, the aggregation function (default \code{mean}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_boxplot()} draws a box-and-whisker per \code{x} category from the raw \code{y}
values (box = Q1-Q3, median line, 1.5*IQR whiskers, outlier points).
\code{mark_errorbar()} draws vertical bars from \code{ymin} to \code{ymax} with horizontal
caps; \code{mark_linerange()} omits the caps. \code{mark_summary()} aggregates \code{y} per
\code{x} with \code{fun} (default mean) and draws the result as points.
}
\details{
\code{mark_pointrange()} draws a point at \verb{(x, y)} with a vertical line from \code{ymin}
to \code{ymax} (an identity mark — supply the summary values yourself, e.g. from a
model). \code{mark_crossbar()} draws a box spanning \code{ymin}..\code{ymax} with a
horizontal line at \code{y}. Both are the identity counterparts of the aggregating
\code{mark_interval()}.
}
\examples{
vplot(mtcars) |> mark_boxplot(x = factor(cyl), y = mpg)
}
