% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_ellipse}
\alias{mark_ellipse}
\alias{mark_hull}
\title{Group summary regions}
\usage{
mark_ellipse(
  plot,
  ...,
  type = "t",
  level = 0.95,
  segments = 51L,
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_hull(plot, ..., blend = NULL, sketch = NULL, data = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, and optionally \code{color}/\code{fill}
(which also splits the points into per-group regions).}

\item{type}{Ellipse type: \code{"t"}, \code{"norm"}, or \code{"euclid"}.}

\item{level}{Confidence level (\code{"t"}/\code{"norm"}) or circle radius (\code{"euclid"}).}

\item{segments}{Number of line segments approximating the ellipse.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Marks that summarise a set of \verb{(x, y)} points (per group, when a \code{color} or
\code{fill} is mapped) as a single enclosing region drawn over the raw points.
}
\details{
\itemize{
\item \code{mark_ellipse()} draws a covariance ellipse: a \code{t} distribution
(\code{type = "t"}, the default, robustly estimated via \pkg{MASS}), a
multivariate normal (\code{"norm"}), or a Euclidean circle of radius \code{level}
(\code{"euclid"}). The algorithm follows ggplot2's \code{stat_ellipse()}.
\item \code{mark_hull()} draws the convex hull of the points.
}

Both are unfilled by default (a boundary), matching ggplot2; map or set a
\code{fill} to shade the region. A region needs at least 3 points per group;
smaller groups are skipped with a warning.
}
\examples{
vplot(iris) |>
  mark_point(x = Sepal.Length, y = Sepal.Width, color = Species) |>
  mark_ellipse(x = Sepal.Length, y = Sepal.Width, color = Species)
vplot(iris) |>
  mark_point(x = Sepal.Length, y = Sepal.Width, color = Species) |>
  mark_hull(x = Sepal.Length, y = Sepal.Width, color = Species)
}
