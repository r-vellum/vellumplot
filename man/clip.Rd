% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip.R
\name{clip}
\alias{clip}
\alias{clip_to}
\alias{clip_layer}
\alias{set_mask}
\title{Clip or mask a plot to a geometry}
\usage{
clip_to(plot, region, invert = FALSE)

clip_layer(plot, region, invert = FALSE)

set_mask(plot, region = NULL, type = c("luminance", "alpha"), feather = 0.35)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{region}{The clip geometry: an \code{sf} object (its polygons), or a
\code{data.frame} / matrix with \code{x} / \code{y} columns (and an optional \code{group} column
for several rings). For \code{set_mask()}, \code{NULL} (default) fades the whole panel.}

\item{invert}{For \code{clip_to()}, \code{TRUE} keeps the marks \emph{outside} \code{region}
(punching the shape out as a hole) instead of inside.}

\item{type}{For \code{set_mask()}, how the mask's pixels set coverage: \code{"luminance"}
(default -- brightness, white shows / black hides) or \code{"alpha"}.}

\item{feather}{For \code{set_mask()}, the soft-edge fraction of the radial ramp in
\verb{[0, 0.9]}: \code{0} a hard disc, larger a gentler fade. Default \code{0.35}.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Restrict where a plot's marks show, by a shape rather than the panel
rectangle. \code{clip_to()} is a \strong{hard} clip: only marks inside \code{region} are
drawn (or, with \code{invert = TRUE}, only those outside) -- the way to fill a
raster / hexbin / tile heatmap into a country outline (a textured
choropleth). \code{set_mask()} is a \strong{soft} mask: a radial luminance ramp that
fades the panel out towards its edges (a vignette / spotlight).
}
\details{
\code{clip_to()} / \code{set_mask()} mask the \strong{whole panel}; \code{clip_layer()} masks only
the \strong{most-recently-added layer} (the one just piped in), so one raster layer
can be clipped to a shape while the axes and other layers stay full-bleed.

All three attach to the plot and resolve at render into an isolated masked
layer (\code{\link[vellum:as_mask]{vellum::as_mask()}}), so the static output is unchanged when none is
set. Cartesian coordinates only (not polar or a nonlinear \code{coord_trans()}).

A smooth \emph{feathered} edge on an arbitrary polygon needs a blur the renderer
does not provide yet, so \code{clip_to()} is hard-edged; use \code{set_mask()} for a
soft (radial) fade.
}
\examples{
\dontrun{
# clip a raster heatmap to a country outline
vplot(grid) |> mark_raster(x = x, y = y, fill = z) |> clip_to(country_sf)

# a vignette
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> set_mask(feather = 0.5)
}
}
