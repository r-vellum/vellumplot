% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparkline.R
\name{vsparkline}
\alias{vsparkline}
\title{Sparklines — tiny word-sized charts}
\usage{
vsparkline(
  values,
  type = c("line", "bar", "winloss"),
  color = "grey30",
  points = c("extremes", "last", "none"),
  point_color = "firebrick",
  baseline = 0,
  win_color = "#2c7fb8",
  loss_color = "#d7301f",
  linewidth = 1,
  point_size = 1.5,
  width = 20,
  height = 6,
  units = "mm",
  dpi = 96
)
}
\arguments{
\item{values}{A numeric vector (the series), length >= 2.}

\item{type}{\code{"line"} (default), \code{"bar"}, or \code{"winloss"}.}

\item{color}{Trend / bar colour. Default \code{"grey30"}.}

\item{points}{For \code{type = "line"}, which points get a dot: \code{"extremes"}
(default -- the min and max), \code{"last"}, or \code{"none"}.}

\item{point_color}{Dot colour (default \code{"firebrick"}).}

\item{baseline}{For \code{"bar"}, the value bars grow from (default \code{0}); for
\code{"winloss"}, the threshold separating a win (\code{>=}) from a loss (default \code{0}).}

\item{win_color, loss_color}{For \code{"winloss"}, the up / down bar colours
(defaults blue / red).}

\item{linewidth}{Trend line width (default \code{1}).}

\item{point_size}{Dot diameter in mm (default \code{1.5}).}

\item{width, height, units}{Physical size of the sparkline; \code{units} is one of
\code{"mm"} (default), \code{"cm"}, \code{"in"}, \code{"pt"}. Default \verb{20 x 6} mm.}

\item{dpi}{Resolution for raster output.}
}
\value{
A \link{PlotSpec}.
}
\description{
\code{vsparkline()} builds a compact, axis-free chart of a single numeric series,
sized in physical units (mm by default) so it reads as a \emph{word-sized graphic}
(Tufte's sparkline) — for a table cell, a caption, or a dashboard tile. It is a
self-contained \link{PlotSpec}: render it with \code{\link[=render_plot]{render_plot()}}, drop it into a
composition with \code{\link[=inset]{inset()}}, or (soon) a table cell.
}
\details{
Three shapes via \code{type}: a \code{"line"} trend (with optional dots on its extremes
or last point), a \code{"bar"} column micro-chart, and a \code{"winloss"} chart of equal
up/down bars about a baseline (for streaks of wins/losses, gains/drops).

The chart fills its box with no axes, gridlines, labels, or legend.
}
\examples{
set.seed(1)
vsparkline(cumsum(rnorm(30)))
vsparkline(rpois(20, 5), type = "bar")
vsparkline(sample(c(-1, 1), 20, replace = TRUE), type = "winloss")
}
\seealso{
\code{\link[=render_plot]{render_plot()}}, \code{\link[=inset]{inset()}}
}
