% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_series_label}
\alias{mark_series_label}
\title{Direct series labels}
\usage{
mark_series_label(
  plot,
  ...,
  size = NULL,
  nudge_x = 2,
  repel = TRUE,
  box_padding = 1,
  min_segment_length = 2,
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): the same \code{x}, \code{y}, and \code{color}/\code{fill} as the
series being labelled.}

\item{size}{Font size in points.}

\item{nudge_x}{Rightward offset (mm) so a label clears its line end (default
\code{2}). Widen the x range (or \code{coord_cartesian(clip = FALSE)}) if labels crowd
the panel edge.}

\item{repel}{Move overlapping labels apart (force-directed, ggrepel-style),
with leader lines to the points? Single cartesian panel only.}

\item{box_padding}{Extra space (mm) kept around each label box during
repulsion.}

\item{min_segment_length}{Shortest leader line (mm) worth drawing; a label
that barely moved gets none.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_series_label()} labels each colour/fill \strong{series at its end} (the point
with the largest \code{x}) with the series name — a legend-free alternative that
reads faster on a multi-line chart. Labels are pushed apart with the same
repel solver as \code{\link[=mark_text]{mark_text()}}. Map the same \code{x}/\code{y}/\code{color} as the lines; the
label text and colour come from the series automatically.
}
\examples{
d <- data.frame(
  t = rep(1:10, 3), v = c(1:10, (1:10) * 1.5, 10:1),
  s = rep(c("a", "b", "c"), each = 10)
)
# Give the panel a little x-room and drop the now-redundant colour legend.
vplot(d) |>
  mark_line(x = t, y = v, color = s) |>
  mark_series_label(x = t, y = v, color = s) |>
  xlim(1, 12) |>
  guides(color = "none")
}
\seealso{
\code{\link[=mark_text]{mark_text()}}
}
