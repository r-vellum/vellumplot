% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{animate}
\alias{animate}
\title{Build a keyframe animation from a plot}
\usage{
animate(plot, nframes = 50, fps = 25)
}
\arguments{
\item{plot}{A \link{PlotSpec} with a \code{\link[=transition_states]{transition_states()}} (and optionally an
\code{\link[=ease_aes]{ease_aes()}}).}

\item{nframes}{Total number of frames to render.}

\item{fps}{Frames per second (the playback rate written into the file).}
}
\value{
A \code{vellum_animation}.
}
\description{
\code{animate()} compiles a plot carrying a \code{\link[=transition_states]{transition_states()}} into one keyframe
scene per state, training the scales once over all states and freezing them,
and returns a \code{vellum_animation}. Write it with \code{\link[=anim_save]{anim_save()}}; printing it
shows the first keyframe as a preview.
}
\details{
Per-state statistics (e.g. a smooth) are recomputed per keyframe, but no scale
domain retrains — the frozen scales are the observable proof the animation
stayed non-reactive (axis breaks do not move between frames).
}
\examples{
\dontrun{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  transition_states(cyl) |>
  animate(nframes = 50, fps = 25)
}
}
\seealso{
\code{\link[=transition_states]{transition_states()}}, \code{\link[=ease_aes]{ease_aes()}}, \code{\link[=anim_save]{anim_save()}}
}
