% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord.R
\name{coord_sf}
\alias{coord_sf}
\title{Map coordinate system}
\usage{
coord_sf(
  plot,
  crs = NULL,
  xlim = NULL,
  ylim = NULL,
  graticule = FALSE,
  graticule_labels = TRUE
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{crs}{Target coordinate reference system to project all layers into
(anything \code{sf::st_crs()} accepts, e.g. an EPSG code, \code{"OGC:CRS84"}, or a
proj/WKT string). \code{NULL} (default) uses the CRS of the first \code{sf} layer. For
guaranteed longitude/latitude order, use \code{"OGC:CRS84"} rather than
\code{EPSG:4326}. For choropleths, prefer an equal-area CRS.}

\item{xlim, ylim}{Length-2 view-window limits in the target CRS, or \code{NULL}.}

\item{graticule}{Draw meridians and parallels behind the map. \code{FALSE}
(default) draws none; \code{TRUE} picks round longitude/latitude breaks
automatically; a list \code{list(lon = , lat = )} sets the breaks explicitly (in
degrees). For a projected CRS the lines are reprojected and therefore curved;
for unprojected longitude/latitude they are straight.}

\item{graticule_labels}{Label the graticule lines with their degree values
(default \code{TRUE}); ignored when \code{graticule} is \code{FALSE}.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{coord_sf()} is the coordinate system for \code{\link[=mark_sf]{mark_sf()}} maps. Before scale
training it reprojects every \code{sf} layer to a common CRS (via
\code{sf::st_transform()}), so the renderer only ever sees projected Cartesian
coordinates, and it locks the panel aspect ratio so the map is not stretched:
\code{1} for a projected CRS, and the equirectangular correction
\code{1/cos(mean_latitude)} for unprojected longitude/latitude data.
}
\details{
\code{sf} is an optional dependency (in \code{Suggests}); \code{coord_sf()} errors with an
install hint if it is not available at render time.
}
\examples{
\dontrun{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
vplot(nc) |> mark_sf(fill = BIR74) |> coord_sf(crs = "OGC:CRS84")
vplot(nc) |> mark_sf(fill = BIR74) |> coord_sf(crs = 3857, graticule = TRUE)
}
}
\seealso{
\code{\link[=mark_sf]{mark_sf()}}, \code{\link[=mark_scalebar]{mark_scalebar()}}, \code{\link[=mark_compass]{mark_compass()}}
}
