% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vgraph.R
\name{vdendrogram}
\alias{vdendrogram}
\title{Dendrogram from a clustering}
\usage{
vdendrogram(
  x,
  direction = c("down", "up", "left", "right"),
  k = NULL,
  labels = TRUE,
  width = 7,
  height = 5,
  dpi = 96
)
}
\arguments{
\item{x}{An \code{hclust} or \code{dendrogram} object.}

\item{direction}{Growth direction (leaves opposite the root): \code{"down"}
(default; root at top), \code{"up"}, \code{"left"}, or \code{"right"}.}

\item{k}{Optionally cut the tree into \code{k} clusters (\code{\link[stats:cutree]{stats::cutree()}}) and
colour branches and leaf labels by cluster; branches above the cut stay
neutral. \code{NULL} (default) draws a single-colour tree.}

\item{labels}{Draw the leaf labels? Default \code{TRUE}.}

\item{width, height, dpi}{Page size (inches) and resolution.}
}
\value{
A \link{PlotSpec}.
}
\description{
\code{vdendrogram()} turns a base \code{hclust}/\code{dendrogram} (e.g. from \code{\link[stats:hclust]{stats::hclust()}})
into a ready node-link dendrogram: leaves spread along a line, each merge drawn
at its height, joined by right-angle brackets. It is a thin preset over
\verb{vgraph(<clustering>, layout = "dendrogram")} plus bracket edges and leaf
labels; for full control, use that path and add marks yourself.
}
\examples{
hc <- hclust(dist(USArrests))
vdendrogram(hc, k = 3)
}
\seealso{
\code{\link[=vgraph]{vgraph()}} for the general node-link path (\code{layout = "dendrogram"}
or \code{"unrooted"}).
}
