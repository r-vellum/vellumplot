% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_halfeye}
\alias{mark_halfeye}
\alias{mark_interval}
\title{Uncertainty marks (slab + interval)}
\usage{
mark_halfeye(
  plot,
  ...,
  .width = c(0.66, 0.95),
  point = "median",
  adjust = 1,
  scale = 0.9,
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_interval(
  plot,
  ...,
  .width = c(0.66, 0.95),
  point = "median",
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): a categorical \code{x} and the sample \code{y}
(+ \code{color}/\code{fill}).}

\item{.width}{Interval probabilities, widest last (default
\code{c(0.66, 0.95)}).}

\item{point}{Central summary: \code{"median"} (default) or \code{"mean"}.}

\item{adjust}{\code{mark_halfeye()} density bandwidth multiplier.}

\item{scale}{Slab width as a fraction of the category band (\code{mark_halfeye()}).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Marks for visualising a distribution's shape \emph{and} its summary intervals
together, in the style of the \pkg{ggdist} package — a natural fit for
posterior draws or bootstrap samples (one row per draw, grouped by a
categorical \code{x}).
}
\details{
\itemize{
\item \code{mark_halfeye()} draws, per \code{x} category, a one-sided density "slab" (a half
violin) with a \strong{point-interval} at its base: the median (or mean) as a
point, a thick bar for the inner interval and a thin bar for the outer one.
\item \code{mark_interval()} draws the point-interval alone (no slab).
}

Intervals are equal-tailed quantile intervals at the \code{.width} probabilities
(widest drawn thinnest). Supply many draws per \code{x} (e.g. posterior samples).
}
\examples{
set.seed(1)
draws <- data.frame(
  grp = rep(c("a", "b", "c"), each = 400),
  val = rnorm(1200, rep(c(0, 1, 2), each = 400))
)
vplot(draws) |> mark_halfeye(x = grp, y = val)
vplot(draws) |> mark_interval(x = grp, y = val)
}
