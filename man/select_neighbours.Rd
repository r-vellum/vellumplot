% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_neighbours}
\alias{select_neighbours}
\title{Highlight a node's graph neighbourhood}
\usage{
select_neighbours(
  plot,
  name = "neighbours",
  on = c("hover", "click"),
  degree = 1L,
  edges = TRUE,
  empty = TRUE
)
}
\arguments{
\item{plot}{A \link{PlotSpec}, normally from \code{\link[=vgraph]{vgraph()}} (piped form), or a selection
\strong{name} string (free-standing form).}

\item{name}{The selection name. Defaults to \code{"neighbours"}.}

\item{on}{The gesture: \code{"hover"} (default) or \code{"click"}.}

\item{degree}{How many hops out from the pointed node to include (\code{1}, the
default, is the immediate neighbourhood; \code{2} adds neighbours-of-neighbours).}

\item{edges}{Whether to include the incident edges in the highlight
(\code{TRUE}, default) or only the nodes.}

\item{empty}{Whether an empty selection matches \emph{all} elements (\code{TRUE},
default — an un-hovered graph shows its full self) or none.}
}
\value{
A \link{PlotSpec} (piped form) or a \code{SelectionSpec} (free-standing form).
}
\description{
A network-aware \code{\link[=select_point]{select_point()}} preset for \code{\link[=vgraph]{vgraph()}} plots: pointing at (or
clicking) a node selects \strong{its neighbourhood} — the node, its incident edges,
and its adjacent nodes — so a host spotlights them and dims the rest. Pointing
at an edge highlights the edge and its two endpoint nodes. It builds on the
node/edge identity \code{vgraph()} emits for an interactive plot (each node keyed by
its vertex \code{name}, each edge carrying its endpoint names); the host
reconstructs the adjacency and projects the gesture across it.
}
\details{
Like the other selections it is \strong{inert on a static render} and enacted by a
capable host (\code{vellumwidget}). On its own it spotlights the neighbourhood; pair
it with \code{\link[=condition]{condition()}} to restyle members explicitly, or \code{\link[=filter_by]{filter_by()}} to show
only the neighbourhood.
}
\examples{
\dontrun{
g <- igraph::make_graph("Zachary")
vgraph(g) |>
  mark_edges() |>
  mark_nodes(size = 3) |>
  select_neighbours(on = "hover")
}
}
\seealso{
\code{\link[=select_point]{select_point()}}, \code{\link[=condition]{condition()}}, \code{\link[=vgraph]{vgraph()}}, \code{\link[=mark_edges]{mark_edges()}}
}
