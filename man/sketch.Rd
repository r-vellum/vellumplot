% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reexports.R
\name{sketch}
\alias{sketch}
\title{Hand-drawn ("sketch") rendering}
\usage{
sketch(
  roughness = 1,
  bowing = 1,
  fill_style = c("hachure", "solid", "crosshatch", "zigzag", "dots"),
  fill_weight = NULL,
  hachure_angle = -41,
  hachure_gap = NULL,
  curve_tightness = 0,
  disable_multi_stroke = FALSE,
  preserve_vertices = FALSE,
  seed = 1L
)
}
\arguments{
\item{roughness}{Wobble amount (\verb{>= 0}; \code{0} is nearly crisp, \code{1} the default,
higher is wilder).}

\item{bowing}{How much straight edges bow.}

\item{fill_style}{One of \code{"hachure"}, \code{"solid"}, \code{"crosshatch"}, \code{"zigzag"},
\code{"dots"}.}

\item{fill_weight, hachure_angle, hachure_gap, curve_tightness, disable_multi_stroke, preserve_vertices, seed}{Further tuning; see \code{\link[vellum:sketch]{vellum::sketch()}}.}
}
\value{
A \code{vellum_sketch} object.
}
\description{
A re-export of \code{\link[vellum:sketch]{vellum::sketch()}} — the one vocabulary quill speaks for the
hand-drawn look (wobbly outlines, hachure fills, à la
\href{https://roughjs.com}{Rough.js}). Pass a \code{sketch()} value to any mark's
\verb{sketch =} argument, to an \code{\link[=element_line]{element_line()}} / \code{\link[=element_rect]{element_rect()}} \verb{sketch =}
slot, or set it plot-wide with \code{\link[=theme_sketch]{theme_sketch()}}:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{vplot(mpg) |> mark_point(x = displ, y = hwy, sketch = sketch(roughness = 1.2))
}\if{html}{\out{</div>}}

Sketch is a geometry property, not a layer \link[=glow]{effect}: it perturbs the
mark itself (its wobble is generated natively in the vellum engine, so it is
exact, cross-backend, and works in PDF), rather than compositing extra copies.
Text is never sketched — pair a handwriting \code{family} with it for a fully
hand-drawn plot.

Resolution is most-specific-wins: a mark's \verb{sketch =} beats an element slot,
which beats the plot-wide \code{\link[=theme_sketch]{theme_sketch()}} default. At any level \code{sketch = NA}
(or \code{FALSE}) forces that element crisp, overriding a broader default;
\code{sketch = NULL} inherits.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg, sketch = sketch(roughness = 1.5, seed = 7))
}
\seealso{
\code{\link[=theme_sketch]{theme_sketch()}}, \code{\link[=mark_point]{mark_point()}}, \code{\link[=element_line]{element_line()}}
}
