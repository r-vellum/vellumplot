% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{lims}
\alias{lims}
\alias{xlim}
\alias{ylim}
\title{Set scale limits with a shortcut}
\usage{
lims(plot, ...)

xlim(plot, ...)

ylim(plot, ...)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{...}{For \code{xlim()}/\code{ylim()}, the limits: two numbers \code{xlim(0, 10)} (or a
length-2 vector) for a continuous axis, or a set of levels
\code{xlim("a", "b", "c")} for a discrete one. For \code{lims()}, named limit vectors,
one per aesthetic, e.g. \code{lims(x = c(0, 10), color = c(0, 100))}.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Convenience wrappers that declare a scale carrying only its limits, instead of
spelling out a full \verb{scale_*()}. \code{xlim()} / \code{ylim()} set the position range;
\code{lims()} sets limits for any named aesthetic. They are shortcuts for
\code{scale_*(limits = ...)}: like there, an explicit range sets the trained
(view) window and marks outside it are clipped.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> xlim(0, 6)
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> lims(x = c(0, 6), y = c(10, 35))
}
\seealso{
\code{\link[=scale_x_continuous]{scale_x_continuous()}}
}
