% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_contour}
\alias{mark_contour}
\alias{mark_contour_filled}
\title{2-D density contours}
\usage{
mark_contour(
  plot,
  ...,
  bins = 10,
  binwidth = NULL,
  breaks = NULL,
  n = 100,
  blend = NULL,
  data = NULL
)

mark_contour_filled(
  plot,
  ...,
  bins = 10,
  binwidth = NULL,
  breaks = NULL,
  n = 100,
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y} (+ optional \code{z} surface, \code{color} /
\code{fill}, \code{linewidth}, \code{linetype}).}

\item{bins}{Target number of contour levels (when neither \code{breaks} nor
\code{binwidth} is given).}

\item{binwidth}{Spacing between contour levels, or \code{NULL}.}

\item{breaks}{Explicit contour levels, or \code{NULL} to derive from \code{bins} /
\code{binwidth}.}

\item{n}{Density-estimate grid resolution per axis (KDE mode only).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_contour()} draws iso-density contour \strong{lines} of a 2-D point cloud
(\code{x}, \code{y}); \code{mark_contour_filled()} fills the bands between them. By default the
field is a kernel density estimate (needs the \pkg{MASS} package); map a \code{z}
aesthetic to instead contour a supplied surface over a regular \code{x}/\code{y} grid.
Contours are coloured by level automatically — \code{mark_contour()} maps
\code{color = after_stat(level)}, \code{mark_contour_filled()} maps \code{fill}. Requires the
\pkg{isoband} package.
}
\examples{
vplot(faithful) |>
  mark_point(x = eruptions, y = waiting) |>
  mark_contour(x = eruptions, y = waiting)
vplot(faithful) |> mark_contour_filled(x = eruptions, y = waiting)
}
