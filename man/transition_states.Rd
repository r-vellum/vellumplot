% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{transition_states}
\alias{transition_states}
\title{Animate a plot across the states of a variable}
\usage{
transition_states(
  plot,
  states,
  transition_length = 1,
  state_length = 1,
  wrap = TRUE
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{states}{A column (bare or a string) whose distinct values, in level
order, are the animation's states.}

\item{transition_length}{Relative duration of the moving segments between
states.}

\item{state_length}{Relative duration of the held pause on each state.}

\item{wrap}{Loop the last state back to the first?}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{transition_states()} turns a plot into a keyframe animation: the plot is
compiled once per level of \code{states} (a "keyframe"), and \code{\link[=animate]{animate()}} tweens the
frames between successive keyframes. The scales are trained \strong{once over all
states and frozen}, so axis breaks, colour ramps and sizes stay put across the
whole animation (the interpolation is non-reactive: the states are fixed at
author time, nothing retrains in response to anything).
}
\details{
The step only records the intent; call \code{\link[=animate]{animate()}} to build the animation and
\code{\link[=anim_save]{anim_save()}} to write it. A plain (non-animated) render ignores it.
}
\examples{
if (requireNamespace("gapminder", quietly = TRUE)) {
  library(gapminder)
  p <- vplot(gapminder) |>
    mark_point(x = gdpPercap, y = lifeExp, size = pop, color = continent) |>
    scale_x_continuous(transform = "log10") |>
    transition_states(year)
}
}
\seealso{
\code{\link[=ease_aes]{ease_aes()}}, \code{\link[=animate]{animate()}}, \code{\link[=anim_save]{anim_save()}}
}
