% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{motion}
\alias{motion}
\alias{echo}
\title{Motion-trail layer effects}
\usage{
motion(
  x = 3,
  y = 0,
  n = 8L,
  alpha = 0.15,
  decay = 1,
  spread = 0,
  blend = "normal",
  color = NULL
)

echo(
  x = 4,
  y = 0,
  n = 3L,
  alpha = 0.45,
  decay = 1,
  spread = 0,
  blend = "normal",
  color = NULL
)
}
\arguments{
\item{x, y}{Trail direction and length in millimetres (\code{+x} right, \code{+y} up):
the offset of the furthest copy. Defaults to a rightward streak.}

\item{n}{Number of trail copies.}

\item{alpha}{Opacity of the nearest (strongest) copy; copies fade toward the
tail.}

\item{decay}{Fade exponent shaping the opacity ramp along the trail (higher =
faster fade toward the tail; \code{0} = no fade).}

\item{spread}{Optional widening, in millimetres, applied progressively toward
the tail (\code{0} keeps a constant width).}

\item{blend}{Blend mode compositing the trail copies (any CSS
\code{mix-blend-mode} name); \code{"normal"} for opaque ghosts.}

\item{color}{Trail colour, or \code{NULL} (default) to inherit the mark's own
resolved colour.}
}
\value{
A \code{MotionSpec} object for a mark's \code{effects} list.
}
\description{
Draw a fading trail of a stroked or point mark: \code{n} copies marching off along
a direction (\code{x}, \code{y}), each further out and fainter than the last, composited
beneath the crisp original — a speed-blur or animation-still look. \code{motion()}
defaults to many close, low-opacity copies (a smooth blur streak); \code{echo()}
defaults to a few wider-spaced, more-opaque copies (discrete ghost repeats).
Both build the same effect, differing only in defaults, and apply to the same
marks as \code{\link[=glow]{glow()}}.
}
\details{
The direction is an \strong{absolute distance in millimetres} (\code{+x} right, \code{+y}
up), resolved device-side via \code{vellum}'s compound \code{npc + mm} unit, so the
trail keeps the same physical length and stays isotropic regardless of the
panel's size or aspect (as \code{\link[=shadow]{shadow()}}'s offset does).
}
\examples{
df <- data.frame(x = 1:20, y = cumsum(rnorm(20)))
vplot(df) |> mark_line(x = x, y = y, effects = list(motion(x = 4)))

vplot(mtcars) |>
  mark_point(x = wt, y = mpg, size = 4, effects = list(echo(x = 5)))
}
\seealso{
\code{\link[=glow]{glow()}}, \code{\link[=shadow]{shadow()}}, \code{\link[=outline]{outline()}}
}
