% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-vegalite.R
\name{spec_to_vegalite}
\alias{spec_to_vegalite}
\alias{spec_from_vegalite}
\title{Convert between a vellumplot spec and a Vega-Lite specification}
\usage{
spec_to_vegalite(plot, json = FALSE)

spec_from_vegalite(vl, data = NULL, env = globalenv())
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{json}{For \code{spec_to_vegalite()}, return a JSON string instead of a list.}

\item{vl}{A Vega-Lite spec as a parsed list or a JSON string.}

\item{data}{Optional data frame for a Vega-Lite spec whose data is external.}

\item{env}{Environment channel expressions are re-quoted in (for
\code{spec_from_vegalite()}).}
}
\value{
\code{spec_to_vegalite()} returns a Vega-Lite spec (a list, or JSON string
if \code{json = TRUE}); \code{spec_from_vegalite()} returns a \link{PlotSpec}.
}
\description{
\code{spec_to_vegalite()} translates a \link{PlotSpec} into a
\href{https://vega.github.io/vega-lite/}{Vega-Lite} specification;
\code{spec_from_vegalite()} translates one back. Both are layered grammars of
graphics, so the mapping is direct for the common cases — marks, encodings,
scales (domain / log-sqrt type / categorical range / title), \code{bin} +
\code{count}, faceting, inline data, and the plot title.
}
\details{
The bridge covers a \strong{documented subset} (see the "Specs, agents, and
interoperability" article). Anything it cannot map — polar/flipped
coordinates, layer effects, patterns, statistical marks without a Vega-Lite
equivalent — is \strong{reported} via a warning listing the dropped features, never
silently diverged.
}
\examples{
p <- vplot(mtcars) |> mark_point(x = wt, y = mpg, color = cyl)
vl <- spec_to_vegalite(p)
vl$mark
}
\seealso{
\code{\link[=as_spec]{as_spec()}}, \code{\link[=spec_to_json]{spec_to_json()}}
}
