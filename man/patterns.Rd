% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern.R
\name{patterns}
\alias{patterns}
\alias{pattern_stripe}
\alias{pattern_crosshatch}
\alias{pattern_grid}
\alias{pattern_dot}
\alias{pattern_checker}
\title{Pattern (hatch) fills}
\usage{
pattern_stripe(
  color = "grey20",
  bg = NA,
  angle = 45,
  spacing = 4,
  linewidth = 1,
  units = "mm"
)

pattern_crosshatch(
  color = "grey20",
  bg = NA,
  angle = 45,
  spacing = 4,
  linewidth = 1,
  units = "mm"
)

pattern_grid(
  color = "grey20",
  bg = NA,
  spacing = 4,
  linewidth = 1,
  units = "mm"
)

pattern_dot(color = "grey20", bg = NA, spacing = 4, size = 1.5, units = "mm")

pattern_checker(color = "grey20", bg = NA, size = 4, units = "mm")
}
\arguments{
\item{color}{Line/dot colour (any R colour). Default \code{"grey20"}.}

\item{bg}{Background fill painted behind the motif, or \code{NA} (default) for a
transparent tile so whatever is under the shape shows through.}

\item{angle}{Stripe orientation in degrees, restricted to \code{0}, \code{45}, \code{90}, or
\code{135} (the orientations that tile seamlessly). Other values error.}

\item{spacing}{Distance between repeats, in \code{units}. Default \code{4}.}

\item{linewidth}{Stripe / grid line width (as elsewhere in the package).
Default \code{1}.}

\item{units}{Length unit for \code{spacing}/\code{size}: \code{"mm"} (default), \code{"in"},
\code{"pt"}, \code{"npc"}, or \code{"native"}.}

\item{size}{For \code{pattern_dot()}, the dot diameter in \code{units}; for
\code{pattern_checker()}, the square size in \code{units}.}
}
\value{
A \code{vellum_pattern} object, usable as a \code{fill} value.
}
\description{
Build a tiling \strong{pattern} to use as a constant \code{fill} value on a filled mark,
the texture counterpart of \code{\link[=linear_gradient]{linear_gradient()}}. Distinguishing regions by
texture (not only hue) keeps a plot legible in greyscale print and under
colour-vision deficiency. Each constructor assembles a small tile and returns a
\code{vellum_pattern} (via \code{\link[vellum:vl_pattern]{vellum::vl_pattern()}}); pass it straight to \code{fill}:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{vplot(df) |> mark_bar(x = g, y = n, fill = pattern_stripe(angle = 45))
}\if{html}{\out{</div>}}

A pattern is an unscaled \emph{value}, not a data-mapped channel (like a gradient).
It applies to any filled mark: bars, areas, ribbons, rects, tiles, boxplots,
violins, ridgelines, half-eyes, hulls/ellipses, and \code{sf} polygons. To vary the
texture \emph{by a variable}, map the \code{pattern} aesthetic instead (a discrete scale).

Patterns render on every backend (PNG, SVG, and — since the tile embeds as an
image — PDF). The tile is rasterised at the scene resolution, so at extreme
magnification it can soften; at ordinary sizes it is crisp.
}
\examples{
df <- data.frame(g = c("a", "b", "c"), n = c(3, 5, 2))
vplot(df) |> mark_bar(x = g, y = n, fill = pattern_crosshatch())
}
\seealso{
\code{\link[=linear_gradient]{linear_gradient()}}, \code{\link[vellum:vl_pattern]{vellum::vl_pattern()}}
}
