% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorations.R
\name{map_decorations}
\alias{map_decorations}
\alias{mark_scalebar}
\alias{mark_compass}
\title{Map decorations}
\usage{
mark_scalebar(
  plot,
  distance = NULL,
  unit = "km",
  position = "bottomleft",
  segments = 4L,
  height = 2.5,
  pad = 4,
  text_size = 7,
  color = "black",
  fill = "white",
  data = NULL
)

mark_compass(
  plot,
  position = "topright",
  size = 10,
  pad = 4,
  rotation = 0,
  color = "black",
  fill = "black",
  text = TRUE,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{distance}{Bar length in \code{unit}. \code{NULL} (default) picks a round number
near a quarter of the panel width.}

\item{unit}{Distance unit for the label and \code{distance}: \code{"km"} (default),
\code{"m"}, \code{"mi"}, or \code{"ft"}.}

\item{position}{Panel corner to anchor the decoration:
\code{"bottomleft"} (scale bar default), \code{"topright"} (compass default),
\code{"bottomright"}, or \code{"topleft"} (the abbreviations \code{"bl"}/\code{"br"}/\code{"tl"}/
\code{"tr"} also work).}

\item{segments}{Number of alternating segments in the bar (default \code{4}).}

\item{height}{Bar thickness, in millimetres.}

\item{pad}{Inset from the panel edge, in millimetres.}

\item{text_size}{Label font size (points).}

\item{color, fill}{Stroke and fill colours. For the scale bar the segments
alternate \code{color} and \code{fill}; for the compass the arrow is \code{fill} with a
\code{color} outline.}

\item{data}{Optional layer data (rarely needed -- decorations are not
data-driven).}

\item{size}{Glyph height, in millimetres.}

\item{rotation}{Extra clockwise rotation of the arrow, in degrees (e.g. to
point at grid north on a rotated layout).}

\item{text}{Draw the \code{"N"} label above the arrow (default \code{TRUE}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Cartographic furniture for \code{\link[=coord_sf]{coord_sf()}} maps. \code{mark_scalebar()} draws a
segmented distance bar; \code{mark_compass()} draws a north arrow. Both are
fixed-position decorations pinned to a panel corner (they take no data
aesthetics). Graticule lines (meridians and parallels) are added through
\code{\link[=coord_sf]{coord_sf()}}'s \code{graticule} argument, not a mark, so they render behind the
map.
}
\examples{
\dontrun{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
vplot(nc) |>
  mark_sf(fill = BIR74) |>
  coord_sf(crs = 3857, graticule = TRUE) |>
  mark_scalebar() |>
  mark_compass()
}
}
\seealso{
\code{\link[=coord_sf]{coord_sf()}}, \code{\link[=mark_sf]{mark_sf()}}
}
