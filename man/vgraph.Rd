% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vgraph.R
\name{vgraph}
\alias{vgraph}
\title{Start a graph (network) plot}
\usage{
vgraph(g, layout = "stress", ..., seed = 42L, width = 6, height = 4, dpi = 96)
}
\arguments{
\item{g}{An \code{igraph} graph (or a two-column edge-list matrix).}

\item{layout}{A layout: a name (\code{"stress"} (default), \code{"sparse_stress"},
\code{"backbone"}, \code{"fr"}, \code{"kk"}, \code{"circle"}, \code{"tree"}, \code{"sugiyama"}, ...), a
supplied \verb{N x 2} coordinate matrix, or a function \code{f(g, ...)} returning one.}

\item{...}{Extra arguments forwarded to the layout function (e.g. \verb{pivots =}
for sparse stress).}

\item{seed}{Integer seed for stochastic layouts (\code{"fr"}, \code{"drl"}), making the
figure reproducible. The global RNG stream is restored afterwards.}

\item{width, height}{Page size in inches.}

\item{dpi}{Output resolution in dots per inch (see \code{\link[=vplot]{vplot()}}).}
}
\value{
A \link{PlotSpec} whose data is the node table and whose \code{edge_data} is the
edge table.
}
\description{
\code{vgraph()} begins a node-link diagram from an \code{igraph} graph. It computes a
layout (vertex \code{x}/\code{y}), builds a node table and an edge table, and returns a
\link{PlotSpec} with the aspect locked (\code{coord_equal}) and a void theme (no axes or
gridlines) -- publication-quality defaults with no tuning. Add layers with
\code{\link[=mark_edges]{mark_edges()}}, \code{\link[=mark_nodes]{mark_nodes()}}, and \code{\link[=mark_node_text]{mark_node_text()}}; edges default to the
edge table and nodes to the node table.
}
\details{
The default layout is stress majorization
(\code{graphlayouts::layout_with_stress}): it is deterministic (same graph -> same
picture) and minimizes the difference between drawn and graph-theoretic
distance. Above \code{10^4} nodes it falls back to sparse stress automatically.
Reciprocal and parallel edges are drawn as parallel straight lines offset off
the centre line (not curved); self-loops as small loops.

\code{igraph} (and \code{graphlayouts}, for its layouts) are optional dependencies (in
\code{Suggests}); \code{vgraph()} errors with an install hint if they are not available.
}
\examples{
\dontrun{
g <- igraph::make_graph("Zachary")
vgraph(g, layout = "stress") |>
  mark_edges() |>
  mark_nodes(size = 3)
}
}
\seealso{
\code{\link[=mark_edges]{mark_edges()}}, \code{\link[=mark_nodes]{mark_nodes()}}, \code{\link[=mark_node_text]{mark_node_text()}}
}
