% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_violin}
\alias{mark_violin}
\alias{mark_ridgeline}
\alias{mark_dotplot}
\title{Density-shape marks}
\usage{
mark_violin(plot, ..., adjust = 1, blend = NULL, sketch = NULL, data = NULL)

mark_ridgeline(
  plot,
  ...,
  adjust = 1,
  height = 1.4,
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_dotplot(
  plot,
  ...,
  binwidth = NULL,
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{...}{Encodings. \code{mark_violin()} needs categorical \code{x} and numeric \code{y};
\code{mark_ridgeline()} needs numeric \code{x} and categorical \code{y}; \code{mark_dotplot()}
needs \code{x}. A mapped \code{color}/\code{fill} sets the shape fill.}

\item{adjust}{Kernel-density bandwidth multiplier (violin/ridgeline).}

\item{blend, sketch, data}{Standard layer arguments (see \code{\link[=mark_point]{mark_point()}}).}

\item{height}{Ridge height as a multiple of the row band (ridgeline; default
\code{1.4}, so adjacent ridges overlap slightly).}

\item{binwidth}{Dot-plot bin width, or \code{NULL} to use ~1/30 of the data range.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Marks that draw a variable's distribution as a filled shape.
\code{mark_violin()} draws a mirrored kernel density of \code{y} per categorical \code{x}
(like a boxplot's footprint); \code{mark_ridgeline()} draws a kernel density of \code{x}
per categorical \code{y} as overlapping ridges. \code{mark_dotplot()} bins \code{x} and
stacks one dot per observation.
}
\examples{
df <- data.frame(g = rep(letters[1:3], each = 50), v = rnorm(150))
vplot(df) |> mark_violin(x = g, y = v)
vplot(df) |> mark_ridgeline(x = v, y = g)
vplot(df) |> mark_dotplot(x = v)
}
