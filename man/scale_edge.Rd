% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_edge}
\alias{scale_edge}
\alias{scale_edge_color}
\alias{scale_edge_colour}
\alias{scale_edge_alpha}
\alias{scale_edge_linetype}
\title{Edge colour / alpha / line-type scales}
\usage{
scale_edge_color(
  plot,
  palette = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL,
  midpoint = NULL
)

scale_edge_colour(
  plot,
  palette = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL,
  midpoint = NULL
)

scale_edge_alpha(plot, range = NULL, limits = NULL, breaks = NULL, name = NULL)

scale_edge_linetype(plot, values = NULL, name = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec}, normally from \code{\link[=vgraph]{vgraph()}}.}

\item{palette}{For \code{scale_edge_color()}, a vector of colours or a single
palette name (as in \code{\link[=scale_color_continuous]{scale_color_continuous()}}); \code{NULL} for a default.}

\item{name}{Legend title, or \code{NULL} to derive from the encoding.}

\item{midpoint}{For \code{scale_edge_color()}, the data value placed at the ramp's
midpoint (a diverging scale); \code{NULL} for an ordinary ramp.}

\item{range}{For \code{scale_edge_alpha()}, the output opacity range \code{c(min, max)},
or \code{NULL} for the default.}

\item{limits, breaks, labels}{Data limits, explicit legend breaks, and explicit
labels, or \code{NULL} to derive from the data.}

\item{values}{For \code{scale_edge_linetype()}, a character vector of line types
(as in \code{\link[=scale_linetype]{scale_linetype()}}); \code{NULL} for the default palette.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Declare the scale for a mapped edge aesthetic on a \code{\link[=vgraph]{vgraph()}} plot -- the
colour (\code{mark_edges(color = )}), opacity (\verb{alpha = }), or line type
(\verb{linetype = }) of the edges. These are the edge counterparts of
\code{\link[=scale_color_continuous]{scale_color_continuous()}} / \code{\link[=scale_alpha]{scale_alpha()}} / \code{\link[=scale_linetype]{scale_linetype()}}: an edge
aesthetic is trained and legended \strong{independently of the node scales}, so a
figure can map, say, node fill to a discrete community \emph{and} edge colour to a
continuous weight without the two collapsing into one legend. Each draws its
own legend automatically.
}
\details{
\code{scale_edge_color()} infers discrete vs continuous from the mapped data (a
factor/character trains a discrete swatch legend; a number a colour bar), the
same as the node colour scale. \code{scale_edge_colour()} is a British-spelling
alias.
}
\examples{
\dontrun{
g <- igraph::make_graph("Zachary")
g <- igraph::set_edge_attr(g, "w", value = runif(igraph::ecount(g)))
vgraph(g) |>
  mark_edges(color = w) |>
  mark_nodes(fill = factor(igraph::membership(igraph::cluster_louvain(g)))) |>
  scale_edge_color(palette = "Grays")
}
}
\seealso{
\code{\link[=mark_edges]{mark_edges()}}, \code{\link[=scale_edge_width]{scale_edge_width()}}, \code{\link[=vgraph]{vgraph()}}
}
