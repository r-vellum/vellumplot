% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waffle.R
\name{vwaffle}
\alias{vwaffle}
\title{Waffle chart}
\usage{
vwaffle(
  data,
  category,
  value = NULL,
  n_cells = 100,
  rows = 10,
  flip = FALSE,
  pad = 0.12,
  width = 5,
  height = 5,
  dpi = 96
)
}
\arguments{
\item{data}{A data frame.}

\item{category}{The categorical column that colours the cells (tidy-eval).}

\item{value}{Optional per-row weight column; if omitted, each row counts once.}

\item{n_cells}{Total number of cells in the grid (default \code{100}). Each
category gets \code{round(share * n_cells)} cells.}

\item{rows}{Number of rows in the grid (default \code{10}); cells fill column by
column from the bottom-left.}

\item{flip}{Fill row by row (left to right) instead of column by column.}

\item{pad}{Gap between cells, as a fraction of a cell (default \code{0.12}).}

\item{width, height, dpi}{Page size (inches) and resolution for the standalone
chart.}
}
\value{
A \link{PlotSpec}.
}
\description{
\code{vwaffle()} draws a \strong{waffle} (square pie): a grid of cells coloured by
category, where each category takes a share of the cells proportional to its
count — a part-of-whole chart that is easier to read than a pie because the
eye counts squares. It is a self-contained chart (like \code{\link[=vsankey]{vsankey()}} /
\code{\link[=vvenn]{vvenn()}}): it returns a \link{PlotSpec} with no axes.
}
\examples{
df <- data.frame(part = c("a", "b", "c"), n = c(50, 30, 20))
vwaffle(df, category = part, value = n)
}
\seealso{
\code{\link[=vvenn]{vvenn()}}, \code{\link[=mark_pie]{mark_pie()}}
}
