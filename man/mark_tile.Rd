% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_tile}
\alias{mark_tile}
\alias{mark_raster}
\alias{mark_bin2d}
\alias{mark_density}
\alias{mark_hex}
\title{Heatmap marks}
\usage{
mark_tile(plot, ..., blend = NULL, data = NULL)

mark_raster(plot, ..., blend = NULL, data = NULL)

mark_bin2d(plot, ..., bins = 30, blend = NULL, data = NULL)

mark_density(plot, ..., adjust = 1, blend = NULL, data = NULL)

mark_hex(plot, ..., bins = 30, blend = NULL, data = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, \code{fill} for tile/raster; \code{x}, \code{y}
for bin2d; \code{x} (+ \code{fill}/\code{color}) for density.}

\item{blend}{Optional blend mode; see \code{\link[=mark_point]{mark_point()}}.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}

\item{bins}{Number of bins per axis for \code{mark_bin2d()} / hex columns for
\code{mark_hex()}.}

\item{adjust}{Bandwidth multiplier for \code{mark_density()}.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_tile()} draws a rectangular tile at each \verb{(x, y)} coloured by \code{fill};
\code{mark_raster()} draws the same as one raster image (a fast path requiring a
complete regular grid). \code{mark_bin2d()} bins continuous \code{x}/\code{y} into a grid and
colours each cell by count. \code{mark_density()} draws a 1-D kernel density of \code{x}
as a filled curve.
}
\examples{
d <- expand.grid(x = 1:5, y = 1:5)
d$z <- d$x * d$y
vplot(d) |> mark_tile(x = x, y = y, fill = z)
}
