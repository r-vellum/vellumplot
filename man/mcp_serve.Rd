% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{mcp_serve}
\alias{mcp_serve}
\title{Run the vellumplot MCP server}
\usage{
mcp_serve(input = "stdin", output = stdout())
}
\arguments{
\item{input, output}{Connections to read requests from / write responses to
(defaults: standard input / output).}
}
\value{
Invisibly \code{NULL}, when the input stream closes.
}
\description{
Starts a Model Context Protocol server on stdio so an agent can generate
validated vellumplot specs. It exposes three tools — \code{get_schema},
\code{list_fields}, and \code{render_spec} — that delegate to \code{\link[=spec_schema]{spec_schema()}},
\code{\link[=spec_fields]{spec_fields()}}, and \code{\link[=spec_diagnose]{spec_diagnose()}} + \code{\link[=render_plot]{render_plot()}}. A \code{render_spec}
failure returns structured diagnostics (unknown field, compile error) so the
agent can repair and retry, rather than a traceback.
}
\details{
The server is pure R (newline-delimited JSON-RPC 2.0 over stdio) and needs no
Node or SDK. Point an MCP client at it with:
\verb{Rscript -e 'vellumplot::mcp_serve()'}, or the bundled launcher
\code{system.file("mcp/server.R", package = "vellumplot")}.
}
\seealso{
\code{\link[=spec_schema]{spec_schema()}}, \code{\link[=spec_fields]{spec_fields()}}, \code{\link[=vplot_from_spec]{vplot_from_spec()}}
}
