% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_text}
\alias{mark_text}
\alias{mark_label}
\title{Text marks}
\usage{
mark_text(
  plot,
  ...,
  size = NULL,
  family = NULL,
  fontface = NULL,
  hjust = "centre",
  vjust = "centre",
  angle = NULL,
  nudge_x = 0,
  nudge_y = 0,
  blend = NULL,
  data = NULL
)

mark_label(
  plot,
  ...,
  size = NULL,
  family = NULL,
  fontface = NULL,
  hjust = "centre",
  vjust = "centre",
  angle = NULL,
  nudge_x = 0,
  nudge_y = 0,
  fill = "white",
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, \code{label} (+ \code{color}, \code{angle}).}

\item{size}{Font size in points.}

\item{family, fontface}{Font family / face (\code{"plain"}, \code{"bold"}, \code{"italic"},
\code{"bold.italic"}).}

\item{hjust, vjust}{Horizontal / vertical justification (constant; \code{"left"},
\code{"centre"}, \code{"right"}, \code{"bottom"}, \code{"top"}, or numeric in \verb{[0, 1]}).}

\item{angle}{Text rotation in degrees.}

\item{nudge_x, nudge_y}{Shift each label by an exact absolute distance in
millimetres (\code{+x} right, \code{+y} up), device-resolved so the nudge is constant
regardless of scale or panel aspect. Default \code{0}.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}

\item{fill}{For \code{mark_label()}, the background fill colour.}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_text()} draws the \code{label} aesthetic as text at each \verb{(x, y)};
\code{mark_label()} adds a filled rounded background behind each label. \code{size} is
the font size in points; \code{angle} (degrees) may be mapped or constant.
}
\examples{
vplot(mtcars) |> mark_text(x = wt, y = mpg, label = rownames(mtcars))
vplot(mtcars) |> mark_point(x = wt, y = mpg) |>
  mark_text(x = wt, y = mpg, label = rownames(mtcars), nudge_y = 2)
}
