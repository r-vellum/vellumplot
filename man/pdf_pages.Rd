% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents.R
\name{pdf_pages}
\alias{pdf_pages}
\title{Write a multi-page PDF}
\usage{
pdf_pages(x, path)
}
\arguments{
\item{x}{Either a \strong{list} of plots (each a \link{PlotSpec}, composition, or table)
— one page each — or a \strong{single faceted} \code{PlotSpec}, in which case it is
split into one page per facet cell (the facet is dropped and the data
filtered per page; each page trains its own scales).}

\item{path}{Output \code{.pdf} path.}
}
\value{
\code{path}, invisibly.
}
\description{
\code{pdf_pages()} writes several plots into one PDF, one plot per page — a report,
a slide deck, or one page per facet. It is the multi-page companion of
\code{\link[=render_plot]{render_plot()}} (which writes a single page). Pages may differ in size (each
plot keeps its own \code{width}/\code{height}), and the per-page accessibility tags
(structure tree + \code{Alt}, see the \emph{Accessibility} article) are written for
every page.
}
\examples{
p1 <- vplot(mtcars) |> mark_point(x = wt, y = mpg)
p2 <- vplot(mtcars) |> mark_histogram(x = mpg, bins = 8)
f <- tempfile(fileext = ".pdf")
pdf_pages(list(p1, p2), f)

# one page per facet cell:
faceted <- vplot(mtcars) |> mark_point(x = wt, y = mpg) |> facet_wrap(~cyl)
pdf_pages(faceted, tempfile(fileext = ".pdf"))
}
\seealso{
\code{\link[=render_plot]{render_plot()}}, \code{\link[=render_all]{render_all()}}
}
