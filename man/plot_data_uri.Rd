% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg.R
\name{plot_data_uri}
\alias{plot_data_uri}
\title{Encode a plot as a data URI}
\usage{
plot_data_uri(
  plot,
  format = c("svg", "png"),
  width = NULL,
  height = NULL,
  dpi = NULL,
  ...
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (or any object \code{\link[=plot_svg]{plot_svg()}} / \code{\link[=render_plot]{render_plot()}}
accepts).}

\item{format}{\code{"svg"} (vector, default) or \code{"png"} (raster).}

\item{width, height}{Optional size override in inches.}

\item{dpi}{For \code{format = "png"}, the render resolution.}

\item{...}{Passed to \code{\link[=plot_svg]{plot_svg()}} (svg) or \code{\link[=render_plot]{render_plot()}} (png).}
}
\value{
A length-1 character \verb{data:} URI.
}
\description{
\code{plot_data_uri()} renders a plot to a self-contained \strong{\verb{data:} URI} string —
the bytes of the image inlined as base64 — ready to drop into an HTML \verb{<img src>}, a Markdown image, an email, or anywhere a URL is expected without a
separate file. \code{"svg"} (default) inlines the crisp vector SVG; \code{"png"} inlines
a raster render.
}
\examples{
uri <- vplot(mtcars) |> mark_point(x = wt, y = mpg) |> plot_data_uri()
substr(uri, 1, 30)
}
\seealso{
\code{\link[=plot_svg]{plot_svg()}}, \code{\link[=render_plot]{render_plot()}}
}
