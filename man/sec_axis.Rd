% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{sec_axis}
\alias{sec_axis}
\alias{dup_axis}
\title{Secondary axes}
\usage{
sec_axis(transform = ~., name = NULL, breaks = NULL, labels = NULL)

dup_axis(transform = ~., name = NULL, breaks = NULL, labels = NULL)
}
\arguments{
\item{transform}{The primary-to-secondary mapping: a formula using \code{.}
(e.g. \code{~ . * 1.8 + 32}), a function, or a \code{\link[scales:transform_log10]{scales::transform_log10()}}-style
transform object. Must be monotonic over the axis range. Defaults to the
identity, \code{~ .}.}

\item{name}{Secondary axis title, or \code{NULL} for none.}

\item{breaks}{Break positions in \strong{secondary} units, or \code{NULL} to compute
them.}

\item{labels}{A character vector of labels (one per break) or a labelling
function, or \code{NULL} for the default number format.}
}
\value{
A \code{SecAxisSpec} to pass to \code{sec.axis}.
}
\description{
Add a secondary axis to a continuous position scale: a second set of ticks and
labels on the opposite edge (top for \code{x}, right for \code{y}), computed as a 1:1
monotonic transform of the primary axis. Pass the result to the \code{sec.axis}
argument of \code{\link[=scale_x_continuous]{scale_x_continuous()}} / \code{\link[=scale_y_continuous]{scale_y_continuous()}}. This is a
labelling convenience (unit conversions, count/proportion dual readouts,
duplicating an axis on a wide plot) --- \strong{not} an independent second axis with
its own data.
}
\details{
\code{sec_axis()} maps the primary axis through \code{transform}; \code{dup_axis()} is the
identity special case (a plain duplicate) with tidy defaults.
}
\section{Limitations (current version)}{

Secondary axes are supported only on continuous position scales under the
default Cartesian coordinate system, with \strong{shared} scales across facets.
Combining \code{sec.axis} with \code{\link[=coord_flip]{coord_flip()}} / \code{\link[=coord_polar]{coord_polar()}} / \code{\link[=coord_trans]{coord_trans()}},
with free facet scales, or with \code{\link[=add_marginal]{add_marginal()}} raises an error. In a plot
composition (\code{|} / \code{/}) the secondary axis is not drawn.
}

\examples{
# Celsius with a Fahrenheit axis on top
vplot(data.frame(t = 0:100, y = (0:100)^2)) |>
  mark_line(x = t, y = y) |>
  scale_x_continuous(name = "°C", sec.axis = sec_axis(~ . * 1.8 + 32, name = "°F"))

# Duplicate the y axis on the right
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  scale_y_continuous(sec.axis = dup_axis())
}
\seealso{
\code{\link[=scale_x_continuous]{scale_x_continuous()}}
}
