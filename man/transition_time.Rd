% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animate.R
\name{transition_time}
\alias{transition_time}
\title{Animate a plot along a continuous time}
\usage{
transition_time(plot, time)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{time}{A numeric column giving each row's time.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{transition_time()} is like \code{\link[=transition_states]{transition_states()}} but treats \code{time} as a
continuous quantity: the states are its distinct values and each transition is
allocated frames \strong{in proportion to its time gap}, so the animation plays at a
constant rate through unevenly spaced times. There is no pause on a state and no
wrap — time flows once, start to finish.
}
\examples{
if (requireNamespace("gapminder", quietly = TRUE)) {
  vplot(gapminder::gapminder) |>
    mark_point(x = gdpPercap, y = lifeExp, size = pop, color = continent) |>
    scale_x_continuous(transform = "log10") |>
    transition_time(year)
}
}
\seealso{
\code{\link[=transition_states]{transition_states()}}, \code{\link[=ease_aes]{ease_aes()}}, \code{\link[=animate]{animate()}}
}
