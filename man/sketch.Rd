% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{sketch}
\alias{sketch}
\title{Hand-drawn (sketch) layer effect}
\usage{
sketch(amount = 0.01, detail = 6L, seed = 1)
}
\arguments{
\item{amount}{Wobble scale, as a fraction of the panel's data range.}

\item{detail}{Number of noise harmonics (higher = busier wobble).}

\item{seed}{Integer seed making the wobble reproducible across renders.}
}
\value{
A \code{SketchSpec} object for a mark's \code{effects} list.
}
\description{
Perturbs a mark's path with smooth low-frequency noise so straight lines
wobble like hand-drawn ink (an XKCD-style look). Applies to path marks:
\code{mark_line()}, \code{mark_step()}, \code{mark_segment()}, \code{mark_area()}, and
\code{mark_ribbon()}. Pairs with \code{\link[=theme_sketch]{theme_sketch()}}.
}
\examples{
df <- data.frame(x = 1:20, y = cumsum(rnorm(20)))
vplot(df) |>
  mark_line(x = x, y = y, effects = list(sketch())) |>
  theme_sketch()
}
\seealso{
\code{\link[=theme_sketch]{theme_sketch()}}
}
