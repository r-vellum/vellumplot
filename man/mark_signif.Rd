% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_signif}
\alias{mark_signif}
\title{Significance brackets}
\usage{
mark_signif(
  plot,
  ...,
  comparisons = NULL,
  method = c("wilcox.test", "t.test"),
  label = c("p", "stars"),
  step = 0.12,
  tip_length = 0.03,
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): a categorical \code{x} and the numeric \code{y}.}

\item{comparisons}{A list of length-2 character vectors naming the group
pairs to test, e.g. \code{list(c("a", "b"), c("b", "c"))}. \code{NULL} (default) tests
each adjacent pair of levels.}

\item{method}{The two-sample test: \code{"wilcox.test"} (default) or \code{"t.test"}.}

\item{label}{\code{"p"} (default, a formatted p-value) or \code{"stars"}
(\code{*}/\verb{**}/\verb{***}/\verb{****}/\code{ns}).}

\item{step}{Vertical gap between stacked brackets, as a fraction of the data's
y range (default \code{0.12}).}

\item{tip_length}{Bracket down-tick length, as a fraction of the y range
(default \code{0.03}).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_signif()} runs a pairwise test between \code{x} groups and draws a
significance \strong{bracket} over each comparison with its p-value (or stars) —
the ggsignif / ggpubr idiom. Add it on top of a \code{mark_boxplot()} /
\code{mark_violin()}; the brackets stack above the data and the y-axis expands to
fit them.
}
\examples{
set.seed(1)
d <- data.frame(g = rep(c("a", "b", "c"), each = 30),
                y = c(rnorm(30), rnorm(30, 1), rnorm(30, 0.4)))
vplot(d) |>
  mark_boxplot(x = g, y = y) |>
  mark_signif(x = g, y = y, comparisons = list(c("a", "b"), c("a", "c")))
}
