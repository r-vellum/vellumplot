% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-fields.R
\name{vplot_ask}
\alias{vplot_ask}
\title{Generate a plot from a natural-language request}
\usage{
vplot_ask(prompt, data, responder, env = globalenv())
}
\arguments{
\item{prompt}{A natural-language description of the desired plot.}

\item{data}{The data frame to plot.}

\item{responder}{A function \verb{function(payload) -> json_string}. \code{payload} is a
list with \code{prompt}, \code{fields} (from \code{\link[=spec_fields]{spec_fields()}}), and \code{schema}.}

\item{env}{Environment channel expressions are re-quoted in.}
}
\value{
A \link{PlotSpec}.
}
\description{
\code{vplot_ask()} is a thin, \strong{model-agnostic} convenience: it assembles the
grounding payload (the JSON \code{\link[=spec_schema]{spec_schema()}} plus \code{\link[=spec_fields]{spec_fields()}} for \code{data}
and the user's \code{prompt}), hands it to a \code{responder} function you supply — any
function that takes the payload and returns a JSON spec string (wrapping the
Claude API, an MCP client, a local model, or a canned fixture) — and validates
the result with \code{\link[=vplot_from_spec]{vplot_from_spec()}}.
}
\details{
vellumplot deliberately ships no built-in model client: the value is the
schema, the grounding, and the validated repair loop, not a hard dependency on
one provider. For a ready-to-run agent integration, point an MCP client at the
bundled server (\code{system.file("mcp/server.R", package = "vellumplot")}).
}
\examples{
\dontshow{if (requireNamespace("jsonlite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# a canned responder standing in for a model call:
responder <- function(payload) {
  spec_to_json(vplot(mtcars) |> mark_point(x = wt, y = mpg))
}
p <- vplot_ask("scatter of weight vs mpg", mtcars, responder)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=spec_fields]{spec_fields()}}, \code{\link[=vplot_from_spec]{vplot_from_spec()}}, \code{\link[=spec_schema]{spec_schema()}}
}
