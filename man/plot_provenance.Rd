% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{plot_provenance}
\alias{plot_provenance}
\title{Inspect the compiled-scene provenance of a plot}
\usage{
plot_provenance(x)
}
\arguments{
\item{x}{A \link{PlotSpec} or plot composition.}
}
\value{
A list of provenance records (see Details). Empty for a plot that
emits no mark grobs.
}
\description{
Compiles \code{x} and returns its \emph{provenance table}: one record per emitted mark
grob, tying each low-level primitive back to the data rows and trained scales
that produced it. Each record's \code{id} matches the grob's \code{data-vellum-id} in
the SVG output (and the \code{id} column of \code{\link[vellum:scene_model]{vellum::scene_model()}}), so it is a
stable join key between the rendered scene and the grammar — the substrate for
interactivity, linked views, and accessibility.
}
\details{
Each record is a plain, serializable list:
\describe{
\item{\code{id}}{stable node id, equal to \code{grob@id} / SVG \code{data-vellum-id} (join key).}
\item{\code{layer}}{1-based layer index within the (sub-)plot spec.}
\item{\code{mark}}{mark type (\code{"point"}, \code{"line"}, …).}
\item{\code{kind}}{\code{"mark"} (the core layer) or \code{"effect"} (a decorative underlay).}
\item{\code{panel}}{facet panel key (\code{"panel-r-c"}), or \code{NA} for a single panel.}
\item{\code{channels}}{aesthetic → trained-scale reference (\code{scale}, \code{type}, \code{domain}).}
\item{\code{rows}}{the original input-data row indices this grob draws.}
}

The \code{rows} field is refined per element for marks that map rows one-to-one to
drawn elements (points, bars, tiles, segments, lines, areas, ribbons, steps,
text, boxplots, sf features, edges); for aggregating marks (histogram,
density, smooth, datashade, …) it is the whole layer, since rows no longer map
to single elements.
}
\examples{
df <- data.frame(wt = mtcars$wt, mpg = mtcars$mpg, model = rownames(mtcars))
p <- vplot(df) |> mark_point(x = wt, y = mpg, data_id = model)
prov <- plot_provenance(p)
prov[[1]]$id
}
\seealso{
\code{\link[vellum:scene_model]{vellum::scene_model()}} and the scene-contract vignette in vellum.
}
