% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_pattern}
\alias{scale_pattern}
\title{Pattern (texture) scale}
\usage{
scale_pattern(plot, values = NULL, name = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{values}{Optional textures, one per level: a character vector of builder
names (\code{"stripe"}, \code{"crosshatch"}, \code{"grid"}, \code{"dot"}, \code{"checker"}), or a list
of \code{vellum_pattern} objects (e.g. from \code{\link[=pattern_stripe]{pattern_stripe()}}). \code{NULL} (default)
cycles a built-in palette of distinct textures.}

\item{name}{Optional legend title.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Map a discrete variable to fill \strong{textures} -- the greyscale- and
colour-vision-safe companion of a fill palette -- via a filled mark's \code{pattern}
aesthetic: \code{mark_bar(x = g, y = n, pattern = series)}. Each level takes a
distinct \code{\link[=pattern_stripe]{pattern_stripe()}}-family texture. Applies to the filled marks (bar,
area, tile, rect, boxplot, violin, hull).
}
\examples{
df <- data.frame(g = c("a", "b", "c"), n = c(3, 5, 2))
vplot(df) |> mark_bar(x = g, y = n, pattern = g) |> scale_pattern()
}
\seealso{
\code{\link[=pattern_stripe]{pattern_stripe()}}, \code{\link[=scale_shape]{scale_shape()}}
}
