% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{condition}
\alias{condition}
\title{Conditional encoding: style by selection membership}
\usage{
condition(selection, if_true, if_false, empty = TRUE)
}
\arguments{
\item{selection}{The selection name (a string) that drives the condition.}

\item{if_true}{The aesthetic value for selection members — a column expression
(data-masked) or a constant.}

\item{if_false}{The value for non-members — a constant, or omitted to use the
theme's dim appearance.}

\item{empty}{Whether an empty selection matches all elements (\code{TRUE}, default).}
}
\value{
Used only inside a \verb{mark_*()} encoding; not called directly.
}
\description{
Use \code{condition()} as the value of a mark aesthetic to make that aesthetic
depend on whether an element is in a \link[=select_point]{selection}: members get
\code{if_true}, non-members get \code{if_false}. This is the general form of
highlight-on-interaction.
}
\details{
\code{condition()} is recognised where it appears in a \verb{mark_*()} encoding; calling
it directly is an error. The \code{if_true} branch is drawn on a static render and
trains scales exactly as the equivalent plain encoding, so
\code{color = condition("s", g, "grey80")} produces the same colour scale and legend
as \code{color = g}. With \code{empty = TRUE} (the default) an empty selection matches
everything, so a static / initial render shows \code{if_true} for all elements and
interaction dims non-members to \code{if_false}.
}
\seealso{
\code{\link[=select_point]{select_point()}}, \code{\link[=filter_by]{filter_by()}}
}
