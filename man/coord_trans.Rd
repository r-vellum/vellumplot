% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord.R
\name{coord_trans}
\alias{coord_trans}
\title{Transformed coordinate system}
\usage{
coord_trans(plot, x = "identity", y = "identity")
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{x, y}{Display transform for that axis (default \code{"identity"}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{coord_trans()} applies a nonlinear transform to the \strong{display} of one or both
position axes, \emph{after} the scale has trained. This differs from a
\verb{scale_*(trans=)}: a scale transform rescales the data and picks its breaks in
the transformed space (so a log scale is labelled \verb{1, 10, 100}), whereas
\code{coord_trans()} keeps the trained breaks at their original data values and only
warps \emph{where} they are drawn — so the axis is still labelled with the raw
values, the gridlines bunch up, and straight lines curve. Use it to show data
on, say, a log display without relabelling the axis in powers of ten.
}
\details{
The transform is separable per axis. Each of \code{x}/\code{y} is a transform name
(\code{"identity"}, \code{"log10"}, \code{"sqrt"}) or a \verb{scales::transform_*()} object. It is
intended for continuous position axes; \code{"reverse"} is better expressed as
\code{scale_*(trans = "reverse")}.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> coord_trans(y = "log10")
}
