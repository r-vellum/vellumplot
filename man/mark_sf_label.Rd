% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_sf_label}
\alias{mark_sf_label}
\title{Label sf features at their interior point}
\usage{
mark_sf_label(
  plot,
  ...,
  size = NULL,
  repel = TRUE,
  box_padding = 1,
  min_segment_length = 2,
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): the \code{label} to draw at each feature, plus an
optional \code{color} / \code{size}. The position comes from the geometry, so \code{x} / \code{y}
are not mapped.}

\item{size}{Font size in points.}

\item{repel}{Move overlapping labels apart (force-directed, ggrepel-style),
with leader lines to the points? Single cartesian panel only.}

\item{box_padding}{Extra space (mm) kept around each label box during
repulsion.}

\item{min_segment_length}{Shortest leader line (mm) worth drawing; a label
that barely moved gets none.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{An \code{sf} data frame (defaults to the plot's). Its geometry supplies
the label positions.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_sf_label()} draws one text label per \code{sf} feature, placed at the
feature's \strong{interior point} (\code{sf::st_point_on_surface()}, guaranteed to fall
inside each polygon --- unlike a centroid, which can land in a bay or a hole).
The label points are reprojected through the \emph{same} \code{coord_sf} CRS as
\code{\link[=mark_sf]{mark_sf()}}, so they always land where the geometry is drawn. Layer it over a
\code{mark_sf()} choropleth to name the regions; \code{repel = TRUE} (the default) pushes
crowded labels apart.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)
  vplot(nc) |>
    mark_sf(fill = AREA) |>
    mark_sf_label(label = NAME, size = 5)
}
}
\seealso{
\code{\link[=mark_sf]{mark_sf()}}, \code{\link[=mark_text]{mark_text()}}
}
