% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{inspect_source}
\alias{inspect_source}
\title{Inspect the data behind a clicked element (click-to-source)}
\usage{
inspect_source(plot, on = c("click", "hover"), values = FALSE)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{on}{The gesture that reveals the source, \code{"click"} (default) or
\code{"hover"}.}

\item{values}{If \code{TRUE}, also ship the referenced data rows so the host can
display their values (a heavier payload); \code{FALSE} (default) ships only row
indices.}
}
\value{
The \link{PlotSpec}, with source inspection registered.
}
\description{
Declare that a host should surface the \strong{source data rows} behind an element
when the user clicks (or hovers) it — the "click-to-source" affordance. It is
inert on a static render; a host (\code{vellumwidget::as_widget()}) reads the
compiled scene's provenance (\code{\link[=provenance_join]{provenance_join()}} / \code{\link[=provenance_payload]{provenance_payload()}}) to
answer the gesture, and under Shiny reports the clicked element's rows to
\verb{input$<id>_source}.
}
\details{
This is opt-in because the row mapping adds to the widget payload; declaring it
on the plot (rather than as a host flag) keeps interactivity a property of the
spec.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  inspect_source()
}
\seealso{
\code{\link[=provenance_payload]{provenance_payload()}}, \code{\link[=select_point]{select_point()}}
}
