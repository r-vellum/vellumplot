% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chord.R
\name{vchord}
\alias{vchord}
\alias{mark_chord}
\title{Chord diagram}
\usage{
vchord(
  data,
  from,
  to,
  value,
  gap = 0.02,
  sort = c("input", "value"),
  link_color = c("source", "target"),
  direction = c("gradient", "gap", "both", "none"),
  label = TRUE,
  width = 6,
  height = 6,
  dpi = 96
)

mark_chord(
  plot,
  from,
  to,
  value,
  gap = 0.02,
  sort = c("input", "value"),
  link_color = c("source", "target"),
  direction = c("gradient", "gap", "both", "none"),
  label = TRUE
)
}
\arguments{
\item{data}{A data frame of flows, or a square numeric matrix (row = from,
column = to).}

\item{from, to, value}{Columns (tidy-eval) for the data-frame form: the source
node, target node, and flow weight. Omit for the matrix form.}

\item{gap}{Gap between sectors, as a fraction of the circle each (default
\code{0.02}).}

\item{sort}{Sector and ribbon order: \code{"input"} (default, first-appearance) or
\code{"value"} (largest weight first).}

\item{link_color}{Colour ribbons by their \code{"source"} (default) or \code{"target"}
node.}

\item{direction}{How to show a ribbon's direction: \code{"gradient"} (default,
fade from opaque at the source to faint at the target), \code{"gap"} (stop the
ribbon short of the target sector, leaving a small gap), \code{"both"}, or
\code{"none"}.}

\item{label}{Label each sector with its node name? Default \code{TRUE}.}

\item{width, height, dpi}{Page size (inches) and resolution.}

\item{plot}{A \link{PlotSpec}.}
}
\value{
A \link{PlotSpec} (\code{vchord()}) or the modified plot (\code{mark_chord()}).
}
\description{
\code{vchord()} draws a weighted flow as a circular chord diagram: one arc
(\emph{sector}) per node, sized by the node's total incident weight, and one ribbon
per flow joining a slice of the source sector to a slice of the target sector
through the centre. Input is a flow list -- \code{from}, \code{to}, \code{value} -- or a
square flow matrix (row = from, column = to; \code{dimnames} are the node names).
Like \code{\link[=vgraph]{vgraph()}} / \code{\link[=vsankey]{vsankey()}} it returns a ready, axis-free, aspect-locked
\link{PlotSpec}; \code{mark_chord()} is the layer it adds.
}
\details{
Flows are directed: each node's arc splits into an outgoing block then an
incoming block, so \code{a -> b} and \code{b -> a} are separate ribbons and a self-flow
\code{a -> a} loops from the node's out-block to its own in-block. Sectors are
coloured by node (a qualitative palette); ribbons take their source node's
colour by default (\code{link_color = "target"} to colour by target).
}
\examples{
flows <- data.frame(
  from = c("A", "A", "B", "C"),
  to = c("B", "C", "C", "A"),
  value = c(3, 2, 4, 1)
)
vchord(flows, from, to, value)
}
