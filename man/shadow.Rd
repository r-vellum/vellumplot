% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{shadow}
\alias{shadow}
\title{Shadow layer effect}
\usage{
shadow(
  x = 0.5,
  y = -0.5,
  color = "black",
  alpha = 0.3,
  spread = 1.5,
  layers = 3L
)
}
\arguments{
\item{x, y}{Shadow offset in millimetres (\code{+x} right, \code{+y} up). Defaults to a
small down-right drop.}

\item{color}{Shadow colour.}

\item{alpha}{Opacity of each copy.}

\item{spread}{Softening spread, in millimetres, over which the copies widen.}

\item{layers}{Number of stacked copies (more = softer).}
}
\value{
A \code{ShadowSpec} object for a mark's \code{effects} list.
}
\description{
Draws dark, low-opacity copies of a stroked or point mark beneath the
original, offset by (\code{x}, \code{y}) and softened by stacking a few widened copies —
a drop shadow (with an offset) or an ambient shadow (offset \code{0}). Applies to
the same marks as \code{\link[=glow]{glow()}}.
}
\details{
The offset is an \strong{absolute distance in millimetres} (\code{+x} right, \code{+y} up),
resolved device-side, so a drop shadow stays the same physical distance and is
isotropic regardless of the panel's size or aspect (via \code{vellum}'s compound
\code{npc + mm} unit).
}
\examples{
df <- data.frame(x = 1:20, y = cumsum(rnorm(20)))
vplot(df) |> mark_line(x = x, y = y, effects = list(shadow()))
}
\seealso{
\code{\link[=glow]{glow()}}, \code{\link[=outline]{outline()}}
}
