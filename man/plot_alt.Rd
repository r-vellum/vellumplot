% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alt.R
\name{plot_alt}
\alias{plot_alt}
\title{Text alternative (alt text) for a plot}
\usage{
plot_alt(x)
}
\arguments{
\item{x}{A \link{PlotSpec} or a plot composition.}
}
\value{
A single string.
}
\description{
Returns the description used as the plot's text alternative for assistive
technology. If \code{\link[=labs]{labs()}} set an explicit \code{alt}, that string is returned
verbatim; otherwise vellumplot generates a prose summary from the specification —
the chart type, what each axis / colour / size encodes, the number of
observations, and any faceting.
}
\details{
\code{\link[=render_plot]{render_plot()}} (and any compile through \code{\link[vellum:as_vellum_scene]{vellum::as_vellum_scene()}}) passes
this text to the scene's description, so the exported \strong{SVG} carries it in
\verb{<desc>} (with \code{role="img"}) and the exported \strong{PDF} tags the chart as a
\code{Figure} whose \code{Alt} is this text. The plot \strong{title} (from \code{labs(title=)})
becomes the accessible name. See the \emph{Accessibility} article.
}
\examples{
p <- vplot(mtcars) |> mark_point(x = wt, y = mpg, color = hp)
plot_alt(p)
plot_alt(labs(p, alt = "Heavier cars get fewer miles per gallon."))
}
\seealso{
\code{\link[=labs]{labs()}}, \code{\link[vellum:describe]{vellum::describe()}}
}
