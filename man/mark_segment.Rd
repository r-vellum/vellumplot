% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_segment}
\alias{mark_segment}
\title{Segment mark}
\usage{
mark_segment(plot, ..., blend = NULL, effects = list(), data = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, \code{xend}, \code{yend} (+ \code{color},
\code{linewidth}, \code{alpha}).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{effects}{A list of layer render effects applied to the mark at draw
time — \code{\link[=glow]{glow()}}, \code{\link[=outline]{outline()}}, \code{\link[=shadow]{shadow()}} (stroked / point marks),
\code{\link[=sketch]{sketch()}} (path marks), and \code{\link[=inner_glow]{inner_glow()}} / \code{\link[=inner_shadow]{inner_shadow()}} (filled
marks). Each effect errors if it does not apply to the mark.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_segment()} draws a straight line from \verb{(x, y)} to \verb{(xend, yend)} per row.
}
\examples{
d <- data.frame(x = 1, y = 1, xend = 5, yend = 4)
vplot(d) |> mark_segment(x = x, y = y, xend = xend, yend = yend)
}
