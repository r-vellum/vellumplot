% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_point}
\alias{mark_point}
\alias{mark_line}
\alias{mark_rule}
\alias{mark_bar}
\title{Add marks to a plot}
\usage{
mark_point(
  plot,
  ...,
  size = NULL,
  shape = NULL,
  position = "identity",
  auto = FALSE,
  seed = NULL,
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)

mark_line(
  plot,
  ...,
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)

mark_rule(
  plot,
  ...,
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)

mark_bar(
  plot,
  ...,
  position = "stack",
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings: named channel expressions such as \code{x}, \code{y}, \code{color},
\code{fill}, \code{size}, \code{shape}, \code{alpha}.}

\item{size, shape}{Convenience arguments for the point size (in mm) / shape;
may be a constant or a mapped expression. One of \code{"circle"}, \code{"square"},
\code{"triangle"}, \code{"diamond"}, \code{"plus"}, \code{"cross"}.}

\item{position}{Position adjustment: \code{"identity"} (default), \code{"jitter"}
(points), or \code{"stack"} / \code{"dodge"} / \code{"fill"} (bars).}

\item{auto}{For \code{mark_point()}, when \code{TRUE} and the layer has very many rows,
automatically render it as a datashaded density raster (see
\code{\link[=mark_datashade]{mark_datashade()}}) instead of individual markers.}

\item{seed}{For \code{mark_point(position = "jitter")}, an optional integer seed
making the jitter reproducible. The global RNG stream is restored afterwards.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{effects}{A list of layer render effects applied to the mark at draw
time — \code{\link[=glow]{glow()}}, \code{\link[=outline]{outline()}}, and \code{\link[=shadow]{shadow()}}. Available on stroked and point
marks.}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Each \verb{mark_*()} appends a drawing layer to a \link{PlotSpec}. Encodings are bare
column names (or expressions) captured with tidy evaluation, e.g.
\verb{x = wt, y = mpg, color = hp}. Scalar values (e.g. \code{size = 3},
\code{color = "red"}) are treated as constant aesthetics rather than data
mappings.
}
\details{
\code{mark_bar()} draws bars from a zero baseline. With an explicit \code{y} it uses the
\code{y} values as heights; with no \code{y} it counts rows per category (the \code{"count"}
stat). When \code{color}/\code{fill} is mapped, grouped bars are stacked by default; use
\code{position = "dodge"} for side-by-side bars or \code{"fill"} to normalise to 1.
}
\section{Interactivity}{

Any mark accepts reserved, per-row arguments (captured like encodings, via tidy
evaluation) that make its elements addressable — and stylable — by an
interactive host without changing what a static render draws:
\itemize{
\item \code{data_id} — a per-element \strong{data key} (e.g. \code{data_id = model}). Emitted by
the SVG backend as \code{data-key} on each element and returned by
\code{vellum::scene_model()}; it is the join key a host uses to map a hover/click
back to a datum, and to link the same datum across views.
\item \code{tooltip} — per-element tooltip text (a column expression or a constant),
surfaced in \code{scene_model()} metadata.
\item \code{hover_group} — a field grouping elements for linked emphasis (consumed by a
host in a later phase).
\item \code{hover_color}, \code{selected_color} — per-element outline colours applied by the
host when the element is hovered / selected (a constant, or mapped from a
column so different marks highlight differently). They override the widget-wide
theme set by \code{vellumwidget::as_widget(hover_color=, selected_color=)}.
}

These are inert for PNG/PDF and for an SVG opened without a JS host: a plot
with none of them compiles and renders exactly as before. Declaring any of them
without \code{data_id} defaults the key to the row index, so the element is still
addressable. They currently apply to \code{stat = "identity"} marks (points, bars,
tiles, segments, edges, hexbins, sf features, …); aggregating stats
(histogram/count/density) drop them, since rows no longer map 1:1 to elements.

How these flow into the vellum scene (the \code{scene_model()} element table, the
SVG \code{data-key} / \verb{data-vellum-*} attributes, and the reserved \code{meta} key
vocabulary) is described in vellum's "The scene contract" vignette
(\code{vignette("scene-contract", package = "vellum")}).
}

\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg, color = hp)

# Declare interactivity (inert on a static render):
df <- data.frame(wt = mtcars$wt, mpg = mtcars$mpg, model = rownames(mtcars))
vplot(df) |> mark_point(x = wt, y = mpg, tooltip = model, data_id = model)
}
