% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_outlier_label}
\alias{mark_outlier_label}
\title{Label the outliers}
\usage{
mark_outlier_label(
  plot,
  ...,
  method = c("iqr", "sd"),
  k = 1.5,
  size = NULL,
  repel = TRUE,
  box_padding = 1,
  min_segment_length = 2,
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, an optional \code{label} (what to write
on each outlier), and optionally \code{color} / \code{fill} to detect outliers within
each group.}

\item{method}{Outlier rule: \code{"iqr"} (Tukey's, the default) or \code{"sd"}.}

\item{k}{Threshold multiplier — IQR whisker length (default \code{1.5}) or number
of standard deviations.}

\item{size}{Font size in points.}

\item{repel}{Move overlapping labels apart (force-directed, ggrepel-style),
with leader lines to the points? Single cartesian panel only.}

\item{box_padding}{Extra space (mm) kept around each label box during
repulsion.}

\item{min_segment_length}{Shortest leader line (mm) worth drawing; a label
that barely moved gets none.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_outlier_label()} labels only the \strong{outlying} points — the extremes,
where the story usually is — instead of every datum. It keeps the rows whose
\code{y} is an outlier (per colour/fill group) and labels just those, repelled
apart with the same solver as \code{\link[=mark_text]{mark_text()}}. Layer it over the raw points.
}
\details{
The default \code{method = "iqr"} flags \code{y} outside \verb{[Q1 - k*IQR, Q3 + k*IQR]} (the
boxplot whisker rule); \code{method = "sd"} flags \verb{|y - mean| > k * sd}. Map a
\code{label} to name each outlier; with none mapped, the outlier's \code{y} value is the
label.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  mark_outlier_label(x = wt, y = mpg, label = rownames(mtcars))
}
\seealso{
\code{\link[=mark_text]{mark_text()}}, \code{\link[=mark_boxplot]{mark_boxplot()}}
}
