% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{plot_manifest}
\alias{plot_manifest}
\title{A reproducibility manifest for a plot}
\usage{
plot_manifest(x)
}
\arguments{
\item{x}{A \link{PlotSpec}.}
}
\value{
A named list: \code{version}, \code{data} (a \code{hash}/\code{nrow}/\code{columns} record),
\code{spec_hash}, \code{n_elements}, and \code{fonts} (the faces the plot resolved to, via
\code{\link[vellum:font_pin]{vellum::font_pin()}}, each a \code{path}/\code{index}/\code{glyphs} record). The fonts are
what lets \code{\link[=plot_verify]{plot_verify()}} tell a font-stack difference apart from a data
change — the same pixels are only guaranteed when the same fonts are
present.
}
\description{
\code{plot_manifest()} returns a small, serializable fingerprint of a plot: a hash
of its input data (order- and column-sensitive), the data's shape and column
names, a structural hash of the spec, and the number of emitted elements. It
is what \code{\link[=plot_svg]{plot_svg()}} can embed (with \code{manifest = TRUE}) so a figure carries
its own provenance, and what \code{\link[=plot_verify]{plot_verify()}} recomputes to confirm a figure
still matches its data.
}
\examples{
plot_manifest(vplot(mtcars) |> mark_point(x = wt, y = mpg))$data$hash
}
\seealso{
\code{\link[=plot_verify]{plot_verify()}}, \code{\link[=plot_svg]{plot_svg()}}, \code{\link[=provenance_join]{provenance_join()}}
}
