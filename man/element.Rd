% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elements.R
\name{element}
\alias{element}
\alias{element_text}
\alias{element_line}
\alias{element_rect}
\alias{element_blank}
\title{Theme elements}
\usage{
element_text(
  family = NULL,
  face = NULL,
  colour = NULL,
  color = NULL,
  size = NULL,
  cex = NULL,
  hjust = NULL,
  vjust = NULL,
  angle = NULL,
  lineheight = NULL,
  margin = NULL
)

element_line(
  colour = NULL,
  color = NULL,
  linewidth = NULL,
  linetype = NULL,
  lineend = NULL,
  sketch = NULL
)

element_rect(
  fill = NULL,
  colour = NULL,
  color = NULL,
  linewidth = NULL,
  linetype = NULL,
  sketch = NULL
)

element_blank()
}
\arguments{
\item{family, face, size, cex, colour, color, hjust, vjust, angle, lineheight, margin}{Text properties. \code{color} is an alias for \code{colour}; \code{size} is in points and
\code{cex} is a relative multiplier on the inherited \code{size}; \code{margin} is a
numeric vector of millimetres (recycled to length 4).}

\item{linewidth, linetype, lineend}{Line properties.}

\item{sketch}{For \code{\link[=element_line]{element_line()}} / \code{\link[=element_rect]{element_rect()}}, a \code{\link[=sketch]{sketch()}} spec giving
that theme element (gridlines, axis lines, ticks, panel/strip/legend
backgrounds) a hand-drawn look, \code{NA} to force it crisp, or \code{NULL} (default)
to inherit from its parent element / the plot-wide \code{\link[=theme_sketch]{theme_sketch()}} default.
Text elements are never sketched.}

\item{fill}{Fill colour (rectangles).}
}
\value{
An element object for use in \code{\link[=theme]{theme()}}.
}
\description{
Typed building blocks for \code{\link[=theme]{theme()}}. Each describes how a family of theme
slots is drawn; any property left \code{NULL} is inherited from the slot's parent
in the theme tree. \code{element_blank()} draws nothing.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  theme(plot.title = element_text(size = 16), panel.grid.minor = element_blank())
}
