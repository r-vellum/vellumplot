% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-fields.R
\name{spec_fields}
\alias{spec_fields}
\title{Summarise a data frame's fields for a model}
\usage{
spec_fields(data, n_examples = 3L)
}
\arguments{
\item{data}{A data frame.}

\item{n_examples}{How many example values to show per field (default 3).}
}
\value{
A data frame with columns \code{name}, \code{type}, \code{class}, \code{n_unique},
\code{examples}.
}
\description{
\code{spec_fields()} returns one row per column of \code{data} with its name, inferred
encoding \code{type} (\code{quantitative} / \code{nominal} / \code{ordinal} / \code{temporal}), R
class, distinct-value count, and a few example values. It is the \emph{grounding}
step for LLM plot generation (\code{\link[=vplot_ask]{vplot_ask()}}, the MCP \code{list_fields} tool):
handing a model the real vocabulary up front is what prevents hallucinated
column names, and \code{\link[=vplot_from_spec]{vplot_from_spec()}} then validates against exactly these.
}
\examples{
spec_fields(mtcars)
}
\seealso{
\code{\link[=vplot_from_spec]{vplot_from_spec()}}, \code{\link[=spec_schema]{spec_schema()}}
}
