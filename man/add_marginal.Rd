% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{add_marginal}
\alias{add_marginal}
\title{Add marginal distributions to a plot}
\usage{
add_marginal(
  plot,
  type = c("density", "histogram"),
  sides = "tr",
  size = 0.15,
  adjust = 1,
  bins = 30,
  group = FALSE
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}) with at least one \code{x}/\code{y} layer.}

\item{type}{The marginal distribution: \code{"density"} (a kernel-density curve,
the default) or \code{"histogram"} (binned counts).}

\item{sides}{Which edges to draw, as a string of \code{"t"} (top, the \code{x}
distribution) and/or \code{"r"} (right, the \code{y} distribution). Default \code{"tr"}.}

\item{size}{The marginal size as a fraction of the panel, in \verb{(0, 1)}.}

\item{adjust}{Bandwidth multiplier for \code{type = "density"} (see \code{\link[=mark_density]{mark_density()}}).}

\item{bins}{Number of bins for \code{type = "histogram"} (see \code{\link[=mark_histogram]{mark_histogram()}}).}

\item{group}{When \code{TRUE} and the plot maps \code{color}/\code{fill} to a discrete
variable, draw one distribution per group in the matching colour (like
\code{ggMarginal(groupColour = TRUE)}). The scatter's legend already covers them,
so no extra legend is added.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{add_marginal()} draws a distribution of the panel's \code{x} variable along the
top edge and/or of its \code{y} variable along the right edge, each sharing the
main panel's axis so it lines up with the scatter (the vellumplot analogue of
\code{ggExtra::ggMarginal()}). It is a plot-level modifier, like \code{\link[=facet_wrap]{facet_wrap()}} or
\code{\link[=coord_flip]{coord_flip()}}: it takes no encoding of its own and instead reads \code{x}, \code{y}
(and, with \code{group = TRUE}, \code{color}) from the first layer that maps a numeric
\code{x} and \code{y} (typically your \code{\link[=mark_point]{mark_point()}}).
}
\details{
The marginals are drawn without their own axes or background. This version
supports a single panel only: combining it with \code{\link[=facet_wrap]{facet_wrap()}} /
\code{\link[=facet_grid]{facet_grid()}}, a non-Cartesian coordinate system (\code{\link[=coord_flip]{coord_flip()}},
\code{\link[=coord_polar]{coord_polar()}}, \code{\link[=coord_fixed]{coord_fixed()}}, \code{coord_sf()}), or a locked aspect ratio is
an error.
}
\examples{
vplot(faithful) |>
  mark_point(x = eruptions, y = waiting) |>
  add_marginal()

vplot(faithful) |>
  mark_point(x = eruptions, y = waiting) |>
  add_marginal(type = "histogram", sides = "t")
}
\seealso{
\code{\link[=mark_density]{mark_density()}}, \code{\link[=mark_histogram]{mark_histogram()}}, \code{\link[=facet_wrap]{facet_wrap()}}
}
