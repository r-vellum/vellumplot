% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_abline}
\alias{mark_abline}
\alias{mark_function}
\title{Reference lines and function curves}
\usage{
mark_abline(
  plot,
  ...,
  slope = 1,
  intercept = 0,
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_function(
  plot,
  ...,
  fun,
  n = 101,
  args = list(),
  blend = NULL,
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{...}{For \code{mark_abline()}/\code{mark_function()}, styling passed through as
constants (e.g. \code{color}, \code{linewidth}, \code{linetype}).}

\item{slope, intercept}{For \code{mark_abline()}, the line's slope and intercept
(each may be a vector to draw several lines). Defaults \code{1} and \code{0}.}

\item{blend, sketch, data}{See \code{\link[=mark_point]{mark_point()}}.}

\item{fun}{For \code{mark_function()}, a function (or its name) mapping a numeric
\code{x} vector to \code{y}, e.g. \code{dnorm} or \verb{\\(x) x^2}.}

\item{n}{For \code{mark_function()}, the number of points sampled across the x
range. Default \code{101}.}

\item{args}{For \code{mark_function()}, extra arguments passed to \code{fun} (e.g.
\code{args = list(mean = 5, sd = 2)}).}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_abline()} draws a sloped reference line \code{y = slope * x + intercept}
across the panel (the diagonal cousin of \code{mark_rule()}'s horizontal/vertical
lines). \code{mark_function()} draws a curve \code{y = fun(x)} sampled over the panel's
x range — e.g. a theoretical density over a histogram, or a reference curve
over a scatter. Both take no data encodings; they reuse the panel's existing
x/y scales, so add them on top of a data layer. Both assume linear axes.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> mark_abline(slope = -5, intercept = 37)
vplot(data.frame(x = rnorm(200))) |>
  mark_histogram(x = x, y = after_stat(density)) |>
  mark_function(fun = dnorm)
}
