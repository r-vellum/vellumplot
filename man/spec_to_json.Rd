% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-json.R
\name{spec_to_json}
\alias{spec_to_json}
\alias{spec_from_json}
\title{Serialize a plot to / from a JSON spec string}
\usage{
spec_to_json(plot, pretty = TRUE)

spec_from_json(json, data = NULL, env = globalenv())
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{pretty}{Whether to pretty-print (default \code{TRUE}).}

\item{json}{A JSON spec string (or a path to a \code{.json} file).}

\item{data, env}{Passed to \code{\link[=from_spec]{from_spec()}} — a data frame for a by-reference
spec, and the environment channel expressions are re-quoted in.}
}
\value{
\code{spec_to_json()} returns a length-1 JSON string; \code{spec_from_json()}
returns a \link{PlotSpec}.
}
\description{
\code{spec_to_json()} renders a \link{PlotSpec} as a JSON string (via \code{\link[=as_spec]{as_spec()}});
\code{spec_from_json()} parses one back into a \code{PlotSpec} (via \code{\link[=from_spec]{from_spec()}}). This
is the portable wire format for the LLM / agent tooling and the Vega-Lite
bridge. See \code{\link[=as_spec]{as_spec()}} for the serializable subset and its guarantees.
}
\examples{
\dontshow{if (requireNamespace("jsonlite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
p <- vplot(mtcars) |> mark_point(x = wt, y = mpg)
json <- spec_to_json(p)
p2 <- spec_from_json(json)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=as_spec]{as_spec()}}, \code{\link[=spec_schema]{spec_schema()}}, \code{\link[=vplot_from_spec]{vplot_from_spec()}}
}
