% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_edge_width}
\alias{scale_edge_width}
\title{Edge-width scale}
\usage{
scale_edge_width(
  plot,
  range = c(0.3, 3),
  limits = NULL,
  breaks = NULL,
  name = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{range}{Output line-width range \code{c(min, max)}.}

\item{limits}{Data limits \code{c(min, max)}, or \code{NULL} to train from the data.}

\item{breaks}{Explicit legend breaks, or \code{NULL}.}

\item{name}{Legend title, or \code{NULL} to derive from the encoding.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Declare the scale for a mapped edge \code{linewidth} aesthetic (e.g.
\code{mark_edges(linewidth = weight)} on a \code{\link[=vgraph]{vgraph()}} plot). The data range is
rescaled to a line-width range and an edge-width legend is drawn automatically.
}
\examples{
\dontrun{
g <- igraph::sample_gnp(20, 0.2)
g <- igraph::set_edge_attr(g, "w", value = runif(igraph::ecount(g)))
vgraph(g) |> mark_edges(linewidth = w) |> mark_nodes() |> scale_edge_width(range = c(0.3, 4))
}
}
\seealso{
\code{\link[=mark_edges]{mark_edges()}}, \code{\link[=vgraph]{vgraph()}}
}
