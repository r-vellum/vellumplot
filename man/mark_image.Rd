% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_image}
\alias{mark_image}
\title{Draw images at data points}
\usage{
mark_image(
  plot,
  ...,
  src = NULL,
  size = 5,
  nudge_x = 0,
  nudge_y = 0,
  interpolate = TRUE,
  blend = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x}, \code{y}, and \code{src} (the image path
column).}

\item{src}{Image source: a column of file paths (mapped, one per datum) or a
single constant path (the same image at every point).}

\item{size}{Image height in millimetres (constant or mapped); the width
follows the image's own aspect ratio. Default \code{5}.}

\item{nudge_x, nudge_y}{Shift each image by an exact absolute distance in
millimetres (\code{+x} right, \code{+y} up). Default \code{0}.}

\item{interpolate}{Smoothly interpolate when scaling (default \code{TRUE})?
\code{FALSE} keeps hard pixel edges.}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_image()} draws a bitmap image (e.g. a flag or logo) at each \verb{(x, y)},
replacing the usual point marker. \code{src} is a column of local image file paths
(per datum) or a single constant path (the same image at every point). Images
are sized by \code{size} (height in millimetres); each image's native aspect ratio
is preserved.
}
\details{
Reading image files requires the suggested
\href{https://docs.ropensci.org/magick/}{magick} package (which decodes PNG, JPEG,
SVG, GIF and more); \code{mark_image()} errors with an install hint if it is not
available.
}
\examples{
\dontrun{
d <- data.frame(x = 1:3, y = c(2, 1, 3),
                flag = c("de.png", "fr.png", "it.png"))
vplot(d) |> mark_image(x = x, y = y, src = flag, size = 8)
}
}
