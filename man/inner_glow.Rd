% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{inner_glow}
\alias{inner_glow}
\alias{inner_shadow}
\title{Inner glow / inner shadow layer effect}
\usage{
inner_glow(size = 3, color = "white", alpha = 0.6)

inner_shadow(size = 3, color = "black", alpha = 0.5)
}
\arguments{
\item{size}{Band width, in millimetres.}

\item{color}{Band colour.}

\item{alpha}{Band opacity.}
}
\value{
An \code{InnerSpec} object for a mark's \code{effects} list.
}
\description{
For filled marks (\code{mark_area()}, \code{mark_ribbon()}, \code{mark_bar()}), draws a soft
band of light (\code{inner_glow()}) or dark (\code{inner_shadow()}) just \emph{inside} the
fill's edge, by masking a wide boundary stroke to the fill shape. Drawn over
the fill.
}
\examples{
df <- data.frame(x = 1:30, y = cumsum(abs(rnorm(30))))
vplot(df) |>
  mark_area(x = x, y = y, fill = "#0a2a43", effects = list(inner_glow()))
}
\seealso{
\code{\link[=glow]{glow()}}
}
