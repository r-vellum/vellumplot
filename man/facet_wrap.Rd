% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facet.R
\name{facet_wrap}
\alias{facet_wrap}
\alias{facet_grid}
\title{Facet a plot into a grid of panels}
\usage{
facet_wrap(
  plot,
  facets,
  ncol = NULL,
  nrow = NULL,
  scales = c("fixed", "free_x", "free_y", "free")
)

facet_grid(plot, facets, scales = c("fixed", "free_x", "free_y", "free"))
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{facets}{A faceting formula. For \code{facet_wrap()}, one-sided such as
\code{~ cyl} (one or more \code{+}-separated variables); for \code{facet_grid()}, a
two-sided \code{rows ~ cols} (use \code{.} for no faceting on a side).}

\item{ncol, nrow}{Number of columns/rows for \code{facet_wrap()}.}

\item{scales}{One of \code{"fixed"} (default), \code{"free_x"}, \code{"free_y"}, \code{"free"}.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{facet_wrap()} lays panels out in a ribbon wrapped into \code{ncol}/\code{nrow};
\code{facet_grid()} arranges them on a 2-D grid defined by a \code{rows ~ cols}
formula. Position scales are shared across panels by default (so axes align);
pass \code{scales = "free_x"}, \code{"free_y"}, or \code{"free"} to train them per panel.
Colour and size scales (and their legends) are always shared in this version.
}
\examples{
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> facet_wrap(~cyl)
vplot(mtcars) |> mark_point(x = wt, y = mpg) |> facet_grid(am ~ cyl)
}
