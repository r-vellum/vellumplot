% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_x_date}
\alias{scale_x_date}
\alias{scale_y_date}
\alias{scale_x_datetime}
\alias{scale_y_datetime}
\alias{scale_x_time}
\alias{scale_y_time}
\title{Date and time position scales}
\usage{
scale_x_date(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_y_date(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_x_datetime(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_y_datetime(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_x_time(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)

scale_y_time(
  plot,
  limits = NULL,
  date_breaks = NULL,
  date_labels = NULL,
  breaks = NULL,
  labels = NULL,
  name = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{limits}{Length-2 vector giving the axis range (same class as the data),
or \code{NULL} to train from the data.}

\item{date_breaks}{A break interval string, e.g. \code{"1 month"}, \code{"2 weeks"},
\code{"10 years"} (passed to \code{\link[scales:breaks_width]{scales::breaks_width()}}). \code{NULL} uses \code{pretty()}.}

\item{date_labels}{A \code{\link[=strftime]{strftime()}} format string for the tick labels, e.g.
\code{"\%b \%Y"}. \code{NULL} uses the default format.}

\item{breaks, labels}{Explicit break positions / labels (override
\code{date_breaks}/\code{date_labels}), or \code{NULL}.}

\item{name}{Axis title, or \code{NULL} to derive from the encoding.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Position scales for \code{Date} (\verb{scale_*_date}), date-time \code{POSIXct}
(\verb{scale_*_datetime}), and time-of-day / \code{difftime} / \code{hms}
(\verb{scale_*_time}) axes. A \code{Date}/\code{POSIXct} column already gets a sensible date
axis automatically; declare one of these to control the break interval or the
label format.
}
\examples{
df <- data.frame(day = as.Date("2020-01-01") + 0:364, y = cumsum(rnorm(365)))
vplot(df) |>
  mark_line(x = day, y = y) |>
  scale_x_date(date_breaks = "3 months", date_labels = "\%b \%Y")
}
