% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svg.R
\name{plot_svg}
\alias{plot_svg}
\title{Render a plot to a self-contained SVG string}
\usage{
plot_svg(
  plot,
  width = NULL,
  height = NULL,
  scaling = c("fixed", "fit"),
  text = "native"
)
}
\arguments{
\item{plot}{A \link{PlotSpec}, \code{\link[=vtable]{vtable()}}, or composition.}

\item{width, height}{Optional page-size override in \strong{inches} (defaults to the
object's own size).}

\item{scaling}{\code{"fixed"} (default) keeps the pixel size; \code{"fit"} sets the root
\verb{<svg>} \code{width}/\code{height} to \verb{100\%} so it fills its container (the \code{viewBox}
preserves the aspect).}

\item{text}{How glyphs are emitted: \code{"native"} (default) or \code{"outline"} (paths,
for maximum portability).}
}
\value{
A length-1 character SVG string.
}
\description{
\code{plot_svg()} compiles any vellumplot object (a \link{PlotSpec}, \code{\link[=vtable]{vtable()}}, or a
composition) to a stand-alone \verb{<svg>} \strong{string} rather than a file — the way
to embed a chart \emph{inline} in HTML: a \code{\link[=vsparkline]{vsparkline()}} in a \code{gt} / \code{reactable} /
\code{DT} cell, a chart in a Quarto callout, or an inline glyph in a report. The
string carries a \code{viewBox}, so it scales.
}
\examples{
svg <- plot_svg(vsparkline(cumsum(rnorm(20))))
substr(svg, 1, 40)
}
\seealso{
\code{\link[=vsparkline]{vsparkline()}}, \code{\link[=gt_vsparkline]{gt_vsparkline()}}, \code{\link[=render_plot]{render_plot()}}
}
