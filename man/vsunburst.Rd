% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sunburst.R
\name{vsunburst}
\alias{vsunburst}
\alias{mark_sunburst}
\title{Sunburst (radial hierarchy) diagram}
\usage{
vsunburst(
  data,
  id,
  parent,
  value,
  inner.radius = 0,
  width = 6,
  height = 6,
  dpi = 96
)

mark_sunburst(plot, id, parent, value, inner.radius = 0)
}
\arguments{
\item{data}{A data frame describing a hierarchy (a parent list).}

\item{id, parent, value}{Columns (tidy-eval): the node id, its parent id
(\code{NA}/\code{""} for the root), and its value (used for leaves).}

\item{inner.radius}{Central hole radius, a fraction in \verb{[0, 1)}; \code{0} (default)
fills to the centre.}

\item{width, height, dpi}{Page size (inches) and resolution.}

\item{plot}{A \link{PlotSpec}.}
}
\value{
A \link{PlotSpec} (\code{vsunburst()}) or the modified plot (\code{mark_sunburst()}).
}
\description{
\code{vsunburst()} draws a tree as concentric rings of sectors: depth maps to a
ring and each node's angular span is its share of its parent's. Input is a
\emph{parent list} — \code{id}, \code{parent} (\code{NA} at the root), and \code{value} (leaf values;
an internal node's value is the sum of its children). Like \code{\link[=vgraph]{vgraph()}} /
\code{\link[=vsankey]{vsankey()}} it returns a ready, axis-free, aspect-locked \link{PlotSpec};
\code{mark_sunburst()} is the layer it adds.
}
\details{
The root is the centre (not drawn as a wedge); \code{inner.radius} opens a hole.
Nodes are coloured by depth. The input must be a single-rooted tree.
}
\examples{
h <- data.frame(
  id = c("root", "A", "B", "A1", "A2", "B1"),
  parent = c(NA, "root", "root", "A", "A", "B"),
  value = c(NA, NA, NA, 3, 2, 4)
)
vsunburst(h, id, parent, value)
}
