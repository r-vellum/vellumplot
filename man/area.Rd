% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{area}
\alias{area}
\title{Define a layout area for \verb{design =}}
\usage{
area(t, l, b = t, r = l)
}
\arguments{
\item{t, l, b, r}{Top, left, bottom, right cell indices (1-based, inclusive).}
}
\value{
An \code{area} spec.
}
\description{
An \code{area()} is a rectangular block of grid cells (1-based, inclusive) that a
sub-plot occupies. Pass a list of \code{area()}s (one per plot, in order) as
\code{concat(..., design = )} to place plots on an explicit, possibly spanning,
grid.
}
\examples{
a <- vplot(mtcars) |> mark_point(x = wt, y = mpg)
b <- vplot(mtcars) |> mark_point(x = hp, y = mpg)
concat(a, b, design = list(area(1, 1, 1, 2), area(2, 1, 2, 1)))
}
