% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{compose_annotation}
\alias{compose_annotation}
\title{Annotate a composition}
\usage{
compose_annotation(
  plot,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag_levels = NULL,
  tag_prefix = "",
  tag_suffix = ""
)
}
\arguments{
\item{plot}{A \code{PlotComposition} (from \code{\link[=concat]{concat()}} / \code{\link[=hconcat]{hconcat()}} / \code{\link[=vconcat]{vconcat()}}).}

\item{title, subtitle, caption}{Figure-level text (or \code{NULL}).}

\item{tag_levels}{Auto-tag style: \code{"A"}, \code{"a"}, \code{"1"}, \code{"i"}, or \code{"I"}
(\code{NULL} = no tags).}

\item{tag_prefix, tag_suffix}{Strings wrapped around each tag.}
}
\value{
The modified \code{PlotComposition}.
}
\description{
Add figure-level text (a \code{title}, \code{subtitle}, \code{caption}) spanning the whole
composition, and/or auto-tag the sub-plots (\code{A}, \code{B}, \code{C}, …). Because every
\code{PlotComposition} carries its own annotation, this works at any nesting level
(unlike patchwork, where annotation is top-level only).
}
\examples{
a <- vplot(mtcars) |> mark_point(x = wt, y = mpg)
b <- vplot(mtcars) |> mark_point(x = hp, y = mpg)
hconcat(a, b) |> compose_annotation(title = "Fuel economy", tag_levels = "A")
}
\seealso{
\code{\link[=theme]{theme()}}, which also accepts a composition to set the figure-level
chrome (title bands, collected legend, panel spacing, tags).
}
