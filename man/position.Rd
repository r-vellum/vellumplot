% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/positions.R
\name{position}
\alias{position}
\alias{position_nudge}
\alias{position_jitter}
\alias{position_dodge}
\alias{position_dodge2}
\alias{position_sina}
\alias{position_jitterdodge}
\title{Position adjustments}
\usage{
position_nudge(x = 0, y = 0)

position_jitter(width = NULL, height = NULL, seed = NULL)

position_dodge(width = NULL)

position_dodge2(padding = 0.1)

position_sina(width = 0.8, seed = NULL)

position_jitterdodge(
  jitter.width = NULL,
  jitter.height = 0,
  dodge.width = 0.75,
  seed = NULL
)
}
\arguments{
\item{x, y}{\code{position_nudge()} shift (data units).}

\item{width, height}{Maximum jitter (data units); \code{NULL} uses the default. For
\code{position_sina()}, \code{width} is the spread as a fraction of the band.}

\item{seed}{Optional integer seed for the random jitter.}

\item{padding}{\code{position_dodge2()} gap between dodged elements, as a fraction
of the element width.}

\item{dodge.width, jitter.width, jitter.height}{\code{position_jitterdodge()}
dodge / jitter extents.}
}
\value{
A \code{vellumplot_position} object for a mark's \code{position} argument.
}
\description{
Fine-grained control over how a mark's overlapping elements are placed. Pass
the result to a mark's \code{position} argument (e.g.
\code{mark_point(position = position_jitter(width = 0.2))}). A bare string
(\code{position = "dodge"}) still works and uses the defaults.
}
\details{
\itemize{
\item \code{position_nudge()} shifts every element by a constant amount in \strong{data}
units (for continuous axes) — handy for offsetting labels from points.
\item \code{position_jitter()} adds uniform random noise; \code{width}/\code{height} are the
maximum shift in data units (default: 40\% of the resolution), \code{seed} makes
it reproducible.
\item \code{position_dodge()} places grouped elements side by side; \code{width} is the
total data-space width shared by the group (default: the category band).
\item \code{position_dodge2()} dodges by the groups actually present at each x and
splits the band between them with a \code{padding} gap — so ragged groupings
stay centred and evenly spaced.
\item \code{position_jitterdodge()} dodges grouped elements, then jitters within each
dodged slot (points over dodged boxes).
\item \code{position_sina()} spreads each category's points along x by a quasirandom
offset scaled to the local y-density, so the cloud traces the distribution's
shape (ggforce's sina). \code{width} is the maximum spread as a fraction of the
category band (default \code{0.8}).
}
}
\examples{
vplot(mtcars) |>
  mark_point(x = factor(cyl), y = mpg, position = position_jitter(width = 0.15))
}
