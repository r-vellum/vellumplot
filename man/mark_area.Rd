% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marks.R
\name{mark_area}
\alias{mark_area}
\alias{mark_ribbon}
\alias{mark_step}
\title{Area, ribbon, and step marks}
\usage{
mark_area(
  plot,
  ...,
  position = "stack",
  blend = NULL,
  sketch = NULL,
  data = NULL
)

mark_ribbon(plot, ..., blend = NULL, sketch = NULL, data = NULL)

mark_step(
  plot,
  ...,
  direction = "hv",
  blend = NULL,
  effects = list(),
  sketch = NULL,
  data = NULL
)
}
\arguments{
\item{plot}{A \link{PlotSpec} (from \code{\link[=vplot]{vplot()}}).}

\item{...}{Encodings (tidy-eval): \code{x} and \code{y} for area/step; \code{x}, \code{ymin},
\code{ymax} for ribbon; plus \code{color}/\code{fill}/\code{alpha}.}

\item{position}{For \code{mark_area()}, how areas sharing an \code{x} combine when
\code{fill}/\code{color} is mapped: \code{"stack"} (default) stacks them into a band,
\code{"fill"} normalises each \code{x} to 1, \code{"identity"} overlays them from the zero
baseline. With no fill mapping all three are equivalent (a single area).}

\item{blend}{Optional blend mode for compositing this layer against what is
already drawn beneath it (the panel and earlier layers), one of the CSS
\code{mix-blend-mode} names, e.g. \code{"multiply"}, \code{"screen"}, \code{"darken"}. The whole
layer composites as one isolated group (not per element).}

\item{sketch}{A \code{\link[=sketch]{sketch()}} spec giving this layer a hand-drawn look (wobbly
outlines, hachure fills), \code{NA}/\code{FALSE} to force it crisp (overriding a
plot-wide \code{\link[=theme_sketch]{theme_sketch()}}), or \code{NULL} (default) to inherit. Geometry marks
accept it; text, raster, hex and datashade marks do not.}

\item{data}{Optional layer data frame; overrides the plot data for this layer.}

\item{direction}{For \code{mark_step()}, \code{"hv"} (horizontal then vertical, default)
or \code{"vh"}.}

\item{effects}{A list of layer render effects applied to the mark at draw
time — \code{\link[=glow]{glow()}}, \code{\link[=outline]{outline()}}, and \code{\link[=shadow]{shadow()}}. Available on stroked and point
marks.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{mark_area()} fills the region between a \code{y} line and the zero baseline;
\code{mark_ribbon()} fills between \code{ymin} and \code{ymax}; \code{mark_step()} draws a
staircase line. All connect points in \code{x} order.
}
\examples{
vplot(pressure) |> mark_area(x = temperature, y = pressure)
}
