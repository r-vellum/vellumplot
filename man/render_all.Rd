% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents.R
\name{render_all}
\alias{render_all}
\title{Render many plots to separate files, in parallel}
\usage{
render_all(plots, paths, workers = NULL, ...)
}
\arguments{
\item{plots}{A named or unnamed list of plots.}

\item{paths}{Output paths, one per plot (the format of each comes from its
extension, as in \code{\link[=render_plot]{render_plot()}}). As a shortcut, when \code{plots} is \strong{named}
\code{paths} may be a single existing \strong{directory}, and each plot is written to
\verb{<name>.png} inside it.}

\item{workers}{Number of parallel workers; \code{NULL} (default) uses the available
cores. \code{1} forces sequential.}

\item{...}{Passed to each \code{\link[vellum:render]{vellum::render()}} (e.g. \code{text}, \code{cvd}).}
}
\value{
\code{paths} (the resolved file paths), invisibly.
}
\description{
\code{render_all()} renders a list of independent plots to separate files across
CPU cores — small multiples, a batch export, one file per group. The work is
embarrassingly parallel (one whole plot per worker), and the result is
byte-identical to rendering them one by one. Parallelism uses process forks,
so it speeds things up on macOS/Linux and falls back to sequential on Windows;
it is only worth it for several substantial plots.
}
\examples{
plots <- list(
  wt = vplot(mtcars) |> mark_point(x = wt, y = mpg),
  hp = vplot(mtcars) |> mark_point(x = hp, y = mpg)
)
render_all(plots, tempdir())
}
\seealso{
\code{\link[=render_plot]{render_plot()}}, \code{\link[=pdf_pages]{pdf_pages()}}, \code{\link[=repeat_]{repeat_()}}
}
