% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_sketch}
\alias{theme_sketch}
\title{Hand-drawn plot theme}
\usage{
theme_sketch(
  plot,
  roughness = 1,
  bowing = 1,
  fill_style = "hachure",
  seed = 1L,
  font = NULL,
  paper = "#f4ecd8",
  ink = "#2b2b2b"
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{roughness, bowing, fill_style, seed}{Passed to \code{\link[=sketch]{sketch()}} to build the
plot-wide default (see \code{\link[=sketch]{sketch()}} for the full set of knobs).}

\item{font}{Font family for all text (text is not sketched; a handwriting font
such as \code{"Comic Sans MS"} or \code{"Chilanka"} sells the hand-drawn look).
\code{NULL} (default) keeps the system default family.}

\item{paper, ink}{Background and foreground (text/line) colours.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
\code{theme_sketch()} turns a whole plot hand-drawn in one line: it sets a
plot-wide \code{\link[=sketch]{sketch()}} default that every geometry mark and theme element
inherits (wobbly gridlines, axis lines, ticks, and marks), on a warm
paper-toned background. Text is never sketched — pass a handwriting \code{font} to
complete the look.
}
\details{
It is a complete theme (like \code{\link[=theme_cyberpunk]{theme_cyberpunk()}}): the sketch it sets is only
a \emph{default}, so any mark's \verb{sketch =} argument, any \code{\link[=element_line]{element_line()}} /
\code{\link[=element_rect]{element_rect()}} \verb{sketch =} slot, or a \verb{scale_*} override still wins. Force an
individual element crisp with \code{sketch = NA}.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg, color = factor(cyl)) |>
  theme_sketch()

# tune the wobble; a crosshatch-filled bar layer that stays crisp
df <- data.frame(g = c("a", "b", "c"), n = c(3, 5, 2))
vplot(df) |>
  mark_bar(x = g, y = n, fill = g) |>
  theme_sketch(roughness = 1.4, fill_style = "crosshatch", seed = 7)
}
\seealso{
\code{\link[=sketch]{sketch()}}, \code{\link[=theme_gray]{theme_gray()}}, \code{\link[=mark_point]{mark_point()}}
}
