% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern.R
\name{pattern_hatch}
\alias{pattern_hatch}
\title{Crisp vector hatch fill}
\usage{
pattern_hatch(
  color = "grey20",
  bg = NA,
  angle = 45,
  spacing = 3,
  linewidth = 0.75
)
}
\arguments{
\item{color}{Line colour (any R colour). Default \code{"grey20"}.}

\item{bg}{Background fill painted behind the lines, or \code{NA} (default) for a
transparent field so what is under the shape shows through.}

\item{angle}{Line orientation in degrees. Any angle is allowed (unlike the
tile patterns, vector hatch has no seamless-tiling restriction). Default
\code{45}.}

\item{spacing}{Distance between lines, in millimetres. Default \code{3}.}

\item{linewidth}{Line width. Default \code{0.75}.}
}
\value{
A \code{vellum_hatch} object, usable as a \code{fill} value.
}
\description{
\code{pattern_hatch()} is the \emph{vector} companion of the tile patterns above:
evenly spaced parallel lines produced as real geometry via \code{\link[vellum:vl_hatch]{vellum::vl_hatch()}}
rather than a rasterised tile. Because it is vector, it stays crisp at any
magnification and in PDF (no embedded image), which makes it the better
redundant, non-colour encoding for accessible and print output — the "fix it"
step alongside \code{\link[=plot_lint]{plot_lint()}} and \code{render_plot(cvd = )}.
}
\details{
Use it exactly like the other pattern constructors: as a constant \code{fill} on a
filled mark, or as one of the \code{values} of \code{\link[=scale_pattern]{scale_pattern()}} to map a discrete
variable to hatch textures.
}
\examples{
df <- data.frame(g = c("a", "b", "c"), n = c(3, 5, 2))
vplot(df) |> mark_bar(x = g, y = n, fill = pattern_hatch(angle = 30))
}
\seealso{
\link{patterns}, \code{\link[=scale_pattern]{scale_pattern()}}, \code{\link[=plot_lint]{plot_lint()}}, \code{\link[vellum:vl_hatch]{vellum::vl_hatch()}}
}
