% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labs.R
\name{labs}
\alias{labs}
\title{Set plot titles and axis/legend labels}
\usage{
labs(
  plot,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  tag = NULL,
  x = NULL,
  y = NULL,
  color = NULL,
  colour = NULL,
  fill = NULL,
  size = NULL,
  alt = NULL,
  ...
)
}
\arguments{
\item{plot}{A \link{PlotSpec}.}

\item{title, subtitle, caption, tag}{Plot-level text (or \code{NULL} to leave unset).}

\item{x, y, size}{Axis / legend title overrides for those aesthetics.}

\item{color, colour, fill}{Colour-scale title override; \code{colour} and \code{fill} are
aliases for \code{color}.}

\item{alt}{A text alternative (alt text) describing the plot for screen
readers and other assistive technology. Overrides the description quill
generates automatically; see \code{\link[=plot_alt]{plot_alt()}}. Emitted into the accessible SVG
(\verb{<desc>}) and tagged PDF (Figure \code{Alt}) by \code{\link[=render_plot]{render_plot()}}.}

\item{...}{Reserved; passing anything here is an error.}
}
\value{
The modified \link{PlotSpec}.
}
\description{
Add plot-level text — a \code{title}, \code{subtitle}, \code{caption}, and \code{tag} — and
override the titles of individual axes and legends (\code{x}, \code{y}, \code{color}, \code{size}).
The title, subtitle, and tag are drawn in a band above the panels; the caption
in a band below. Repeated \code{labs()} calls merge, with later values winning.
}
\details{
Axis/legend overrides set here are used unless a matching \verb{scale_*(name = )}
is also given, which takes precedence. With neither, the title is derived from
the mapping.
}
\examples{
vplot(mtcars) |>
  mark_point(x = wt, y = mpg) |>
  labs(title = "Fuel economy", x = "Weight", y = "MPG")
}
