% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{PlotSpec}
\alias{PlotSpec}
\title{The plot specification}
\usage{
PlotSpec(
  data = NULL,
  edge_data = NULL,
  layers = list(),
  scales = list(),
  facet = NULL,
  coord = NULL,
  resolve = list(),
  width = 6,
  height = 4,
  dpi = 96,
  theme = NULL,
  labels = list(),
  marginal = NULL
)
}
\arguments{
\item{data}{The data frame.}

\item{edge_data}{For a graph plot (from \code{\link[=vgraph]{vgraph()}}), the edge table; the
default data for \code{\link[=mark_edges]{mark_edges()}}. \code{NULL} for ordinary plots.}

\item{layers}{A list of layer specifications (one per \verb{mark_*()}).}

\item{scales}{A list of declared scale overrides.}

\item{facet}{A faceting specification (from \code{\link[=facet_wrap]{facet_wrap()}} / \code{\link[=facet_grid]{facet_grid()}}),
or \code{NULL} for a single panel.}

\item{coord}{A coordinate specification (from \code{\link[=coord_cartesian]{coord_cartesian()}} /
\code{\link[=coord_flip]{coord_flip()}} / \code{\link[=coord_fixed]{coord_fixed()}}), or \code{NULL} for the default Cartesian system.}

\item{resolve}{A named list mapping an aesthetic to \code{"shared"} or
\code{"independent"} (the scale-resolution lattice; see \code{\link[=resolve_scale]{resolve_scale()}}).}

\item{width, height}{Page size in inches.}

\item{dpi}{Output resolution in dots per inch (pixels per inch). The exported
PNG's pixel dimensions are \code{width * dpi} by \code{height * dpi}.}

\item{theme}{A theme (a named list of resolved element/setting overrides, from
\code{\link[=theme]{theme()}} / a \verb{theme_*()} preset), or \code{NULL} for the default theme.}

\item{labels}{A named list of plot/axis/legend label overrides (see \code{\link[=labs]{labs()}}).}

\item{marginal}{A marginal-distribution specification (from \code{\link[=add_marginal]{add_marginal()}}),
or \code{NULL} for no marginals.}
}
\value{
A \code{PlotSpec}.
}
\description{
\code{PlotSpec} is the S7 class that \code{\link[=vplot]{vplot()}} creates and the \verb{mark_*()} /
\verb{scale_*()} functions extend. It is a plain, inspectable, serializable data
object: data, a list of layers, a list of scale overrides, and the page size.
Nothing is drawn until it is compiled with \code{\link[vellum:as_vellum_scene]{vellum::as_vellum_scene()}} (e.g.
via \code{\link[=render_plot]{render_plot()}}). Printing it draws the plot; inspect its structure with
\code{\link[=summary]{summary()}}.
}
\seealso{
\code{\link[=vplot]{vplot()}}, \code{\link[=mark_point]{mark_point()}}, \code{\link[=scale_x_continuous]{scale_x_continuous()}}
}
