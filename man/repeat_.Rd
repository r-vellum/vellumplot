% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repeat.R
\name{repeat_}
\alias{repeat_}
\title{Repeat a view across fields}
\usage{
repeat_(plot, ..., ncol = NULL, nrow = NULL)
}
\arguments{
\item{plot}{A \link{PlotSpec}; the repeated aesthetic(s) are set on every layer
(added if not already mapped).}

\item{...}{Named aesthetics, each a character vector of field names, e.g.
\code{x = c("wt", "hp", "disp")}.}

\item{ncol, nrow}{Grid dimensions (passed to \code{\link[=concat]{concat()}}).}
}
\value{
A \code{PlotComposition}.
}
\description{
Replicate a plot, re-pointing one or more encodings at a different data field
each time, and arrange the copies as a composition (like \code{\link[=concat]{concat()}}, so each
sub-plot keeps its own scales and axes). Supply each aesthetic as a character
vector of column names; all vectors must be the same length \code{N}, and are
zipped to produce \code{N} sub-plots.
}
\examples{
repeat_(vplot(mtcars) |> mark_point(y = mpg), x = c("wt", "hp", "disp"))
}
